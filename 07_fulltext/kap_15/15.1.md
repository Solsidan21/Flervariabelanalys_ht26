# Section 15.1: Double Integrals
> Studieanteckningar baserade på Adams & Essex, Calculus 9th ed, sid 815-820
> Kursens numrering (Adams 10:e uppl.) = 15.1. Motsvarar 14.1 i 9:e uppl., som texten är hämtad ur.

## Chapter 15 Introduction

This chapter extends the concept of integration to functions of two variables. We have seen how the Riemann integral of a one-variable function was defined in terms of Riemann sums. In a similar manner, the double integral of a two-variable function over a region in the xy-plane will be defined using Riemann sums of values f(x_ij, y_ij) of the function at points in the region. As you will see, many geometric and physical applications of integration to three-dimensional situations lead naturally to double integrals.

---

## Section 15.1: Double Integrals

[sid 815]

Let us start with the case where D is a closed rectangle with sides parallel to the coordinate axes in the xy-plane, and f is a bounded function on D. If D consists of the points (x, y) such that a <= x <= b and c <= y <= d, we can form a partition P of D into small rectangles by partitioning each of the intervals [a, b] and [c, d], say by points

    a = x_0 < x_1 < x_2 < ... < x_(m-1) < x_m = b,
    c = y_0 < y_1 < y_2 < ... < y_(n-1) < y_n = d.

The partition P of D then consists of the mn rectangles R_ij (1 <= i <= m, 1 <= j <= n), consisting of points (x, y) for which x_(i-1) <= x <= x_i and y_(j-1) <= y <= y_j. (See Figure 15.1.)

[FIGUR 15.1: A partition of a rectangle D into smaller subrectangles R_ij.]

The rectangle R_ij has area

    A_ij = Δx_i Δy_j = (x_i - x_(i-1))(y_j - y_(j-1))

and diameter (i.e., diagonal length)

    diam(R_ij) = sqrt((Δx_i)^2 + (Δy_j)^2) = sqrt((x_i - x_(i-1))^2 + (y_j - y_(j-1))^2).

The norm of the partition P is the largest of these subrectangle diameters:

    ||P|| = max diam(R_ij)
            1 <= i <= m
            1 <= j <= n

Now we pick an arbitrary point (x_ij, y_ij) in each of the rectangles R_ij and form the Riemann sum

    R(f; P) = sum(i=1 to m) sum(j=1 to n) f(x_ij, y_ij) A_ij,

which is the sum of mn terms, one for each rectangle in the partition. (Here, the double summation indicates the sum as i goes from 1 to m of terms, each of which is itself a sum as j goes from 1 to n.) The term corresponding to rectangle R_ij is, if f(x_ij, y_ij) >= 0, the volume of the rectangular box whose base is R_ij and whose height is the value of f at (x_ij, y_ij). Therefore, for positive functions f, the Riemann sum R(f; P) approximates the volume above D and under the graph of f. The double integral of f over D is defined to be the limit of such Riemann sums, provided the limit exists as ||P|| -> 0 independently of how the points (x_ij, y_ij) are chosen. We make this precise in the following definition.

[sid 816]

[sid 817]

## DEFINITION 1 -- The double integral over a rectangle

We say that f is integrable over the rectangle D and has double integral

    I = doubleintegral over D f(x,y) dA;

if for every positive number ε there exists a number δ depending on ε, such that

    |R(f; P) - I| < ε

holds for every partition P of D satisfying ||P|| < δ and for all choices of the points (x_ij, y_ij) in the subrectangles of P.

The dA that appears in the expression for the double integral is an area element. It represents the limit of the ΔA = Δx Δy in the Riemann sum and can also be written dx dy or dy dx, the order being unimportant. When we evaluate double integrals by iteration in the next section, dA will be replaced with a product of differentials dx and dy, and the order will be important.

As is true for functions of one variable, functions that are continuous on D are integrable on D. Of course, many bounded but discontinuous functions are also integrable, but an exact description of the class of integrable functions is beyond the scope of this text.

## EXAMPLE 1

Let D be the square 0 <= x <= 1, 0 <= y <= 1. Use a Riemann sum corresponding to the partition of D into four smaller squares with points selected at the centre of each to find an approximate value for

    doubleintegral over D (x^2 + y) dA.

Solution The required partition P is formed by the lines x = 1/2 and y = 1/2, which divide D into four squares, each of area ΔA = 1/4. The centres of these squares are the points (1/4, 1/4), (1/4, 3/4), (3/4, 1/4), and (3/4, 3/4). Therefore, the required approximation is

    doubleintegral over D (x^2 + y) dA ≈ R(x^2 + y; P) = 13/16 = 0.8125.

[sid 818]

Double Integrals over More General Domains

It is often necessary to use double integrals of bounded functions f(x, y) over domains that are not rectangles. If the domain D is bounded, we can choose a rectangle R with sides parallel to the coordinate axes such that D is contained inside R. If f(x, y) is defined on D, we can extend its domain to be R by defining f(x, y) = 0 for points in R that are outside of D. The integral of f over D can then be defined to be the integral of the extended function over the rectangle R.

## DEFINITION 2

If f(x, y) is defined and bounded on domain D, let f-tilde be the extension of f that is zero everywhere outside D:

    f-tilde(x, y) = { f(x, y), if (x, y) belongs to D
                    { 0,        if (x, y) does not belong to D.

If D is a bounded domain, then it is contained in some rectangle R with sides parallel to the coordinate axes. If f-tilde is integrable over R, we say that f is integrable over D and define the double integral of f over D to be

    doubleintegral over D f(x,y) dA = doubleintegral over R f-tilde(x,y) dA.

This definition makes sense because the values of f-tilde in the part of R outside of D are all zero, so do not contribute anything to the value of the integral. However, even if f is continuous on D, f-tilde will not be continuous on R unless f(x, y) -> 0 as (x, y) approaches the boundary of D. Nevertheless, if f and D are "well-behaved," the integral will exist. We cannot delve too deeply into what constitutes well-behaved, but assert, without proof, the following theorem that will assure us that most of the double integrals we encounter do, in fact, exist.

## THEOREM 1

If f is continuous on a closed, bounded domain D whose boundary consists of finitely many curves of finite length, then f is integrable on D.

According to Theorem 2 of Section 13.1, a continuous function is bounded if its domain is closed and bounded. Generally, however, it is not necessary to restrict our domains to be closed. If D is a bounded domain and int(D) is its interior (an open set), and if f is integrable on D, then

    doubleintegral over D f(x,y) dA = doubleintegral over int(D) f(x,y) dA.

We will discuss improper double integrals of unbounded functions or over unbounded domains in Section 15.3.

[sid 819]

Properties of the Double Integral

Some properties of double integrals are analogous to properties of the one-dimensional definite integral and require little comment: if f and g are integrable over D, and if L and M are constants, then

(a) doubleintegral over D f(x,y) dA = 0 if D has zero area.

(b) Area of a domain: doubleintegral over D 1 dA = area of D (because it is the volume of a cylinder with base D and height 1).

(c) Integrals representing volumes: If f(x, y) >= 0 on D, then doubleintegral over D f(x,y) dA = V >= 0, where V is the volume of the solid lying vertically above D and below the surface z = f(x, y).

(d) If f(x, y) <= 0 on D, then doubleintegral over D f(x,y) dA = -V <= 0, where V is the volume of the solid lying vertically below D and above the surface z = f(x, y).

(e) Linear dependence on the integrand: doubleintegral over D (Lf(x,y) + Mg(x,y)) dA = L doubleintegral over D f(x,y) dA + M doubleintegral over D g(x,y) dA.

(f) Inequalities are preserved: If f(x, y) <= g(x, y) on D, then doubleintegral over D f(x,y) dA <= doubleintegral over D g(x,y) dA.

(g) The triangle inequality: |doubleintegral over D f(x,y) dA| <= doubleintegral over D |f(x,y)| dA.

(h) Additivity of domains: If D_1, D_2, ..., D_k are nonoverlapping domains on each of which f is integrable, then f is integrable over the union D = D_1 ∪ D_2 ∪ ... ∪ D_k and

    doubleintegral over D f(x,y) dA = sum(j=1 to k) doubleintegral over D_j f(x,y) dA.

Nonoverlapping domains can share boundary points but not interior points.

Double Integrals by Inspection

As yet, we have not said anything about how to evaluate a double integral. The main technique for doing this, called iteration, will be developed in the next section, but it is worth pointing out that double integrals can sometimes be evaluated using symmetry arguments or by interpreting them as volumes that we already know.

## EXAMPLE 2

If R is the rectangle a <= x <= b, c <= y <= d, then

    doubleintegral over R 3 dA = 3 * area of R = 3(b - a)(d - c).

Here, the integrand is f(x, y) = 3, and the integral is equal to the volume of the solid box of height 3 whose base is the rectangle R. (See Figure 15.2.)

[FIGUR 15.2: The base of a rectangular box of height 3 above rectangle R.]

[sid 820]

## EXAMPLE 3

Evaluate I = doubleintegral over (x^2 + y^2 <= 1) (sin x + y^3 + 4) dA.

Solution The integral can be expressed as the sum of three integrals by property (e) of double integrals:

    I = doubleintegral over (x^2 + y^2 <= 1) sin x dA + doubleintegral over (x^2 + y^2 <= 1) y^3 dA + doubleintegral over (x^2 + y^2 <= 1) 4 dA
      = I_1 + I_2 + I_3.

The domain of integration is a circular disk of radius 1 centred at the origin. Since f(x, y) = sin x is an odd function of x, its graph bounds as much volume below the xy-plane in the region x < 0 as it does above the xy-plane in the region x > 0. These two contributions to the double integral cancel, so I_1 = 0. Note that symmetry of both the domain and the integrand is necessary for this argument.

Similarly, I_2 = 0 because y^3 is an odd function and D is symmetric about the x-axis.

Finally,

    I_3 = doubleintegral over D 4 dA = 4 * area of D = 4π.

Thus, I = 0 + 0 + 4π = 4π.

## EXAMPLE 4

If D is the disk of Example 3, the integral

    doubleintegral over D sqrt(1 - x^2 - y^2) dA

represents the volume of a hemisphere of radius 1 and so has the value 2π/3.

When evaluating double integrals, always be alert for situations such as those in the above examples. You can save much time by not trying to calculate an integral whose value should be obvious without calculation.

[sid 821]

---

## Exercises 15.1

### Uppgift 1
For I = doubleintegral over D (5 - x - y) dA, where D is the rectangle 0 <= x <= 3, 0 <= y <= 2, and P is the partition of D into six squares of side 1, calculate the Riemann sum for I with (x_ij, y_ij) being the upper-left corner of each square.

### Uppgift 2
For the integral I and partition P in Exercise 1, calculate the Riemann sum with (x_ij, y_ij) being the upper-right corner of each square.

### Uppgift 3
For the integral I and partition P in Exercise 1, calculate the Riemann sum with (x_ij, y_ij) being the lower-left corner of each square.

### Uppgift 4
For the integral I and partition P in Exercise 1, calculate the Riemann sum with (x_ij, y_ij) being the lower-right corner of each square.

### Uppgift 5
For the integral I and partition P in Exercise 1, calculate the Riemann sum with (x_ij, y_ij) being the centre of each square.

### Uppgift 6
For the integral I and partition P in Exercise 1, evaluate I by interpreting it as a volume.

### Uppgift 7
In Exercises 7-10, D is the disk x^2 + y^2 <= 25, and P is the partition of the square -5 <= x <= 5, -5 <= y <= 5 into one hundred 1 × 1 squares. Approximate the double integral

    J = doubleintegral over D f(x,y) dA,

where f(x, y) = 1 by calculating the Riemann sum R(f; P) corresponding to (x_ij, y_ij) being the corner of each square closest to the origin. Hint: Using symmetry will make the job easier.

### Uppgift 8
For the integral J in Exercise 7 with D and P as defined there, approximate J by calculating the Riemann sum R(f; P) with (x_ij, y_ij) being the corner of each square farthest from the origin.

### Uppgift 9
For the integral J in Exercise 7 with D and P as defined there, approximate J by calculating the Riemann sum R(f; P) with (x_ij, y_ij) being the centre of each square.

### Uppgift 10
For the integral J in Exercise 7 with D and P as defined there, evaluate J.

### Uppgift 11
[CAS] Repeat Exercise 5 using the integrand e^x instead of 5 - x - y.

### Uppgift 12
[CAS] Repeat Exercise 9 using f(x, y) = x^2 + y^2 instead of f(x, y) = 1.

### Uppgift 13
Evaluate doubleintegral over R dA, where R is the rectangle -1 <= x <= 3, -4 <= y <= 1.

### Uppgift 14
Evaluate doubleintegral over D (x + 3) dA, where D is the half-disk 0 <= y <= sqrt(4 - x^2).

### Uppgift 15
Evaluate doubleintegral over T (x + y) dA, where T is the parallelogram having the points (2, 2), (1, -1), (-2, -2), and (-1, 1) as vertices.

### Uppgift 16
Evaluate doubleintegral over (|x| + |y| <= 1) (x^3 cos(y^2) + 3 sin y - π) dA.

### Uppgift 17
Evaluate doubleintegral over (x^2 + y^2 <= 1) (4x^2 y^3 - x + 5) dA.

### Uppgift 18
Evaluate doubleintegral over (x^2 + y^2 <= a^2) sqrt(a^2 - x^2 - y^2) dA.

### Uppgift 19
Evaluate doubleintegral over (x^2 + y^2 <= a^2) (a - sqrt(x^2 + y^2)) dA.

### Uppgift 20
Evaluate doubleintegral over S (x + y) dA, where S is the square 0 <= x <= a, 0 <= y <= a.

### Uppgift 21
Evaluate doubleintegral over T (1 - x - y) dA, where T is the triangle with vertices (0, 0), (1, 0), and (0, 1).

### Uppgift 22
Evaluate doubleintegral over R sqrt(b^2 - y^2) dA, where R is the rectangle 0 <= x <= a, 0 <= y <= b.

