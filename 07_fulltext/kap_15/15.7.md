# Section 15.7: Applications of Multiple Integrals
> Studieanteckningar baserade på Adams & Essex, Calculus 9th ed, sid 857-866
> Kursens numrering (Adams 10:e uppl.) = 15.7. Motsvarar 14.7 i 9:e uppl., som texten är hämtad ur.

When we express the volume V of a region R in 3-space as an integral,

    V = tripleintegral over R of dV,

we are regarding V as a "sum" of infinitely many infinitesimal elements of volume, that is, as the limit of the sum of volumes of smaller and smaller nonoverlapping subregions into which we subdivide R. This idea of representing sums of infinitesimal elements of quantities by integrals has many applications.

For example, if a rigid body of constant density rho g/cm^3 occupies a volume V cm^3, then its mass is m = rho·V g. If the density is not constant but varies continuously over the region R of 3-space occupied by the rigid body, say rho = rho(x, y, z), we can still regard the density as being constant on an infinitesimal element of R having volume dV. The mass of this element is therefore dm = rho(x, y, z) dV, and the mass of the whole body is calculated by integrating these mass elements over R:

    m = tripleintegral over R of rho(x, y, z) dV.

Similar formulas apply when the rigid body is one- or two-dimensional, and its density is given in units of mass per unit length or per unit area. In such cases single or double integrals are needed to sum the individual elements of mass. All this works because mass is "additive"; that is, the mass of a composite object is the sum of the masses of the parts that compose the object. The surface areas, gravitational forces, moments, and energies we consider in this section all have this additivity property.

## The Surface Area of a Graph

We can use a double integral over a domain D in the xy-plane to add up surface area elements and thereby calculate the total area of the surface S with equation z = f(x, y) defined for (x, y) in D. We assume that f has continuous first partial derivatives in D, so that S is smooth and has a nonvertical tangent plane at P = (x, y, f(x, y)) for any (x, y) in D. The vector

    n = -f_1(x, y)i - f_2(x, y)j + k

is an upward normal to S at P. An area element dA at position (x, y) in the xy-plane has a vertical projection onto S whose area dS is sec(theta) times the area dA, where theta is the angle between n and k.

Since

    cos(theta) = (n · k) / (|n| |k|) = 1 / sqrt(1 + f_1(x, y)^2 + f_2(x, y)^2),

we have

    dS = sqrt(1 + (partial z/partial x)^2 + (partial z/partial y)^2) dA.

Therefore, the area of S is

    S = doubleintegral over D of sqrt(1 + (partial z/partial x)^2 + (partial z/partial y)^2) dA.

[sid 857]

## EXAMPLE 1

Find the area of that part of the hyperbolic paraboloid z = x^2 - y^2 that lies inside the cylinder x^2 + y^2 = a^2.

Solution Since partial z/partial x = 2x and partial z/partial y = -2y, the surface area element is

    dS = sqrt(1 + 4x^2 + 4y^2) dA = sqrt(1 + 4r^2) r dr d(theta).

The required surface area is the integral of dS over the disk r <= a:

    S = integral from 0 to 2pi of d(theta) integral from 0 to a of sqrt(1 + 4r^2) r dr

Let u = 1 + 4r^2, du = 8r dr:

    S = (2pi) · (1/8) integral from 1 to 1+4a^2 of sqrt(u) du
      = (pi/4) · (2/3) u^(3/2) |_(1 to 1+4a^2)
      = (pi/6) [(1 + 4a^2)^(3/2) - 1] square units.

## The Gravitational Attraction of a Disk

[sid 858]

Newton's universal law of gravitation asserts that two point masses m_1 and m_2, separated by a distance s, attract one another with a force

    F = k m_1 m_2 / s^2,

k being a universal constant. The force on each mass is directed toward the other, along the line joining the two masses. Suppose that a flat disk D of radius a, occupying the region x^2 + y^2 <= a^2 of the xy-plane, has constant areal density sigma (units of mass per unit area). Let us calculate the total force of attraction that this disk exerts upon a mass m located at the point (0, 0, b) on the positive z-axis. The total force is a vector quantity. Although the various mass elements on the disk are in different directions from the mass m, symmetry indicates that the net force will be in the direction toward the centre of the disk, that is, toward the origin. Thus, the total force will be -F k, where F is the magnitude of the force.

We will calculate F by integrating the vertical component dF of the force of attraction on m due to the mass sigma dA in an area element dA on the disk. If the area element is at the point with polar coordinates [r, theta], and if the line from this point to (0, 0, b) makes angle phi with the z-axis, then the vertical component of the force of attraction of the mass element sigma dA on m is

    dF = (k m sigma dA) / (r^2 + b^2) · cos(phi) = k m b sigma dA / (r^2 + b^2)^(3/2).

Accordingly, the total vertical force of attraction of the disk on m is

    F = k m b sigma doubleintegral over D of dA / (r^2 + b^2)^(3/2)
      = k m b sigma integral from 0 to 2pi of d(theta) integral from 0 to a of r dr / (r^2 + b^2)^(3/2)

Let u = r^2 + b^2, du = 2r dr:

    F = k m b sigma integral from b^2 to a^2+b^2 of u^(-3/2) du
      = k m b sigma · (-2/sqrt(u)) |_(b^2 to a^2+b^2)
      = 2 k m sigma [1 - b / sqrt(a^2 + b^2)].

Remark If we let a approach infinity in the above formula, we obtain the formula F = 2 k m sigma for the force of attraction of a plane of areal density sigma on a mass m located at distance b from the plane. Observe that F does not depend on b. Try to reason on physical grounds why this should be so.

Remark The force of attraction on a point mass due to suitably symmetric solid objects (such as balls, cylinders, and cones) having constant density rho (units of mass per unit volume) can be found by integrating elements of force contributed by thin, disk-shaped slices of the solid.


## Moments and Centres of Mass

[sid 859]

The centre of mass of a rigid body is that point (fixed in the body) at which the body can be supported so that in the presence of a constant gravitational field it will not experience any unbalanced torques that will cause it to rotate. The torques experienced by a mass element dm in the body can be expressed in terms of the moments of dm about the three coordinate planes. If the body occupies a region R in 3-space and has continuous volume density rho(x, y, z), then the mass element dm = rho(x, y, z) dV that occupies the volume element dV is said to have moments (x - x_0) dm, (y - y_0) dm, and (z - z_0) dm about the planes x = x_0, y = y_0, and z = z_0, respectively. Thus, the total moments of the body about these three planes are

    M_(x=x_0) = tripleintegral over R of (x - x_0) rho(x, y, z) dV = M_(x=0) - x_0 m
    M_(y=y_0) = tripleintegral over R of (y - y_0) rho(x, y, z) dV = M_(y=0) - y_0 m
    M_(z=z_0) = tripleintegral over R of (z - z_0) rho(x, y, z) dV = M_(z=0) - z_0 m,

where m = tripleintegral over R of rho dV is the mass of the body and M_(x=0), M_(y=0), and M_(z=0) are the moments about the coordinate planes x = 0, y = 0, and z = 0, respectively. The centre of mass P-bar = (x-bar, y-bar, z-bar) of the body is that point for which M_(x=x-bar), M_(y=y-bar), and M_(z=z-bar) are all equal to zero. Thus,

Centre of mass
The centre of mass of a solid occupying region R of 3-space and having continuous density rho(x, y, z) (units of mass per unit volume) is the point (x-bar, y-bar, z-bar) with coordinates given by

    x-bar = M_(x=0) / m = (tripleintegral over R of x dV) / (tripleintegral over R of dV),

    y-bar = M_(y=0) / m = (tripleintegral over R of y dV) / (tripleintegral over R of dV),

    z-bar = M_(z=0) / m = (tripleintegral over R of z dV) / (tripleintegral over R of dV).

These formulas can be combined into a single vector formula for the position vector r-bar = x-bar i + y-bar j + z-bar k of the centre of mass in terms of the position vector r = x i + y j + z k of an arbitrary point in R:

    r-bar = (M_(x=0) i + M_(y=0) j + M_(z=0) k) / m = (tripleintegral over R of rho r dV) / (tripleintegral over R of rho dV),

where the integral of the vector function rho r is understood to mean the vector whose components are the integrals of the components of rho r.

Remark Similar expressions hold for distributions of mass over regions in the plane or over intervals on a line. We use the appropriate areal or line densities and double or single definite integrals.

Remark If the density is constant, it cancels out of the expressions for the centre of mass. In this case the centre of mass is a geometric property of the region R and is called the centroid or centre of gravity of that region.

## EXAMPLE 2

[sid 860]

Find the centroid of the tetrahedron T bounded by the coordinate planes and the plane x/a + y/b + z/c = 1.

Solution The density is assumed to be constant, so we may take it to be unity. The mass of T is thus equal to its volume: m = V = abc/6. The moment of T about the yz-plane is

    M_(x=0) = tripleintegral over T of x dV
            = integral from 0 to a of x dx integral from 0 to b(1-x/a) of dy integral from 0 to c(1-x/a-y/b) of dz
            = c integral from 0 to a of x dx integral from 0 to b(1-x/a) of (1 - x/a - y/b) dy
            = c integral from 0 to a of x dx [y - xy/a - y^2/(2b)] |_(y=0 to b(1-x/a))
            = c integral from 0 to a of x dx [(b(1-x/a) - xb(1-x/a)/a - b^2(1-x/a)^2/(2b)) dx
            = (bc/2) integral from 0 to a of x(1 - x/a)^2 dx
            = (bc/2) [x^2/2 - 2x^3/(3a) + x^4/(4a^2)] |_(x=0 to a)
            = (bc/2) · (a^2 bc/24)
            = a^2 bc/24.

Thus, x-bar = M_(x=0) / m = (a^2 bc/24) / (abc/6) = a/4. By symmetry, the centroid of T is (a/4, b/4, c/4).

## EXAMPLE 3

[sid 861]

Find the centre of mass of a solid occupying the region S that satisfies x >= 0, y >= 0, z >= 0, and x^2 + y^2 + z^2 <= a^2, if the density at distance R from the origin is k R.

Solution The mass of the solid is distributed symmetrically in the first octant part of the ball R <= a so that the centre of mass (x-bar, y-bar, z-bar) must satisfy x-bar = y-bar = z-bar. The mass of the solid is

    m = tripleintegral over S of k R dV = integral from 0 to pi/2 of d(theta) integral from 0 to pi/2 of sin(phi) d(phi) integral from 0 to a of (k R) R^2 dR
      = (pi/2) · 1 · k · (a^4/4)
      = k a^4 / 8.

The moment about the xy-plane is

    M_(z=0) = tripleintegral over S of z k R dV = tripleintegral over S of (k R) R cos(phi) R^2 sin(phi) dR d(phi) d(theta)
            = (k/2) integral from 0 to pi/2 of d(theta) integral from 0 to pi/2 of sin(2phi) d(phi) integral from 0 to a of R^4 dR
            = (k/2) · (pi/2) · 1 · (a^5/5)
            = k a^5 / 20.

Hence, z-bar = (k a^5/20) / (k a^4/8) = 2a/5, and the centre of mass is (2a/5, 2a/5, 2a/5).

## Moment of Inertia

[sid 862]

The kinetic energy of a particle of mass m moving with speed v is

    KE = (1/2) m v^2.

The mass of the particle measures its inertia, which is twice the energy it has when its speed is one unit.

If the particle is moving in a circle of radius D, its motion can be described in terms of its angular speed omega, measured in radians per unit time. In one revolution the particle travels a distance 2π D in time 2π/omega. Thus, its (translational) speed v is related to its angular speed by

    v = D omega.

Suppose that a rigid body is rotating with angular speed omega about an axis L. If (at some instant) the body occupies a region R and has density rho = rho(x, y, z), then each mass element dm = rho dV in the body has kinetic energy

    dKE = (1/2) v^2 dm = (1/2) omega^2 D^2 rho dV,

where D = D(x, y, z) is the perpendicular distance from the volume element dV to the axis of rotation L. The total kinetic energy of the rotating body is therefore

    KE = (1/2) omega^2 tripleintegral over R of D^2 rho dV = (1/2) I omega^2,

where

    I = tripleintegral over R of D^2 rho dV.

I is called the moment of inertia of the rotating body about the axis L. The moment of inertia plays the same role in the expression for kinetic energy of rotation (in terms of angular speed) that the mass does in the expression for kinetic energy of translation (in terms of linear speed). The moment of inertia is twice the kinetic energy of the body when it is rotating with unit angular speed.

If the entire mass of the rotating body were concentrated at a distance D_0 from the axis of rotation, then its kinetic energy would be (1/2) m D_0^2 omega^2. The radius of gyration D-bar is the value of D_0 for which this energy is equal to the actual kinetic energy (1/2) I omega^2 of the rotating body. Thus, m D-bar^2 = I, and the radius of gyration is

    D-bar = sqrt(I/m) = sqrt[ (tripleintegral over R of D^2 rho dV) / (tripleintegral over R of rho dV) ].

## EXAMPLE 4

(The acceleration of a rolling ball)

(a) Find the moment of inertia and radius of gyration of a solid ball of radius a and constant density rho about a diameter of that ball.

(b) With what linear acceleration will the ball roll (without slipping) down a plane inclined at angle alpha to the horizontal?

Solution

(a) We take the z-axis as the diameter and integrate in cylindrical coordinates over the ball B of radius a centred at the origin. Since the density rho is constant, we have

    I = tripleintegral over B of r^2 rho dV
      = integral from 0 to 2pi of d(theta) integral from 0 to a of r^3 dr integral from -sqrt(a^2-r^2) to sqrt(a^2-r^2) of rho dz
      = 4 rho integral from 0 to a of r^3 sqrt(a^2 - r^2) dr

Let u = a^2 - r^2, du = -2r dr:

      = 2 rho integral from 0 to a^2 of (a^2 - u) sqrt(u) du
      = 2 rho [a^2 · (2/3) u^(3/2) - (2/5) u^(5/2)] |_(u=0 to a^2)
      = 2 rho [a^2 · (2/3) a^3 - (2/5) a^5]
      = (8/15) rho a^5.

Since the mass of the ball is m = (4/3) pi a^3 rho, the radius of gyration is

    D-bar = sqrt(I/m) = sqrt((8/15) a^2 / ((4/3) pi)) = sqrt(2/5) a.

(b) We can determine the acceleration of the ball by using conservation of total (kinetic plus potential) energy. When the ball is rolling down the plane with speed v, its centre is moving with speed v and losing height at a rate v sin(alpha). Since the ball is not slipping, it is rotating about a horizontal axis through its centre with angular speed omega = v/a. Hence, its kinetic energy (due to translation and rotation) is

    KE = (1/2) m v^2 + (1/2) I omega^2
       = (1/2) m v^2 + (1/2) · (2/5) m a^2 · (v^2/a^2)
       = (7/10) m v^2.

When the centre of the ball is at height h (above some reference height), the ball has (gravitational) potential energy

    PE = m g h.

(This is the work that must be done against a constant gravitational force F = mg to raise it to height h.) Since total energy is conserved,

    (7/10) m v^2 + m g h = constant.

Differentiating with respect to time t, we obtain

    0 = (7/10) m · 2v · (dv/dt) + m g · (dh/dt)
      = (7/5) m v · (dv/dt) - m g v sin(alpha).

Thus, the ball rolls down the incline with acceleration

    dv/dt = (5/7) g sin(alpha).

Remark Integrals of higher multiplicity. Just like higher-order derivatives, it is easy to imagine the need for multiple integrations beyond just triple integrals. For instance, in physics we must consider both position and momentum of a particle to understand its behaviour. Each of these requires three coordinates, so a total of six coordinates are needed. Integrals may have to be taken over all six.

[sid 863]


Suppose that a rigid body is rotating with angular speed omega about an axis L. Each mass element dm = rho dV in the body has kinetic energy dKE = (1/2) omega^2 D^2 rho dV, where D = D(x,y,z) is the perpendicular distance from dV to L. The total kinetic energy is KE = (1/2) I omega^2, where I = tripleintegral over R of D^2 rho dV is the moment of inertia.

## Exercises 15.7

Surface area problems
Use double integrals to calculate the areas of the surfaces in Exercises 1–9.

### Uppgift 1
The part of the plane z = 2x + 2y inside the cylinder x^2 + y^2 = 1

### Uppgift 2
The part of the plane 5z = 3x - 4y inside the elliptic cylinder x^2 + 4y^2 = 4

### Uppgift 3
The hemisphere z = sqrt(a^2 - x^2 - y^2)

### Uppgift 4
The half-ellipsoidal surface z = 2·sqrt(1 - x^2 - y^2)

### Uppgift 5
The conical surface 3z^2 = x^2 + y^2, 0 <= z <= 2

### Uppgift 6
The paraboloid z = 1 - x^2 - y^2 in the first octant

### Uppgift 7
The part of the surface z = y^2 above the triangle with vertices (0,0), (0,1), and (1,1)

### Uppgift 8
The part of the surface z = sqrt(x) above the region 0 <= x <= 1, 0 <= y <= sqrt(x)

### Uppgift 9
The part of the cylindrical surface x^2 + z^2 = 4 that lies above the region 0 <= x <= 2, 0 <= y <= x

### Uppgift 10
Show that the parts of the surfaces z = 2xy and z = x^2 + y^2 that lie in the same vertical cylinder have the same area.

### Uppgift 11
Show that the area S of the part of the paraboloid z = (1/2)(x^2 + y^2) lying above the square -1 <= x <= 1, -1 <= y <= 1 is given by

    S = (8/3) integral from 0 to pi/4 of (1 + sec^2(theta))^(3/2) d(theta) - 2pi/3,

and use numerical methods to evaluate the area to 3 decimal places.

### Uppgift 12
The canopy shown in Figure 14.58 is the part of the hemisphere of radius sqrt(2) centred at the origin that lies above the square -1 <= x <= 1, -1 <= y <= 1. Find its area. Hint: It is possible to get an exact solution by first finding the area of the part of the sphere x^2 + y^2 + z^2 = 2 that lies above the plane z = 1. If you do the problem directly by integrating the surface area element over the square, you may encounter an integral that you can't evaluate exactly, and you will have to use numerical methods.

Mass and gravitational attraction

### Uppgift 13
Find the mass of a spherical planet of radius a whose density at distance R from the centre is rho = A/(B + R^2).

In Exercises 14–17, find the gravitational attraction that the given object exerts on a mass m located at (0, 0, b). Assume the object has constant density rho. In each case you can obtain the answer by integrating the contributions made by disks of thickness dz, making use of the formula for the attraction exerted by the disk obtained in the text.

### Uppgift 14
The ball x^2 + y^2 + z^2 <= a^2, where a < b

### Uppgift 15
The cylinder x^2 + y^2 <= a^2, 0 <= z <= h, where h < b

### Uppgift 16
The cone 0 <= z <= b - sqrt(x^2 + y^2)/a

### Uppgift 17
The half-ball 0 <= z <= sqrt(a^2 - x^2 - y^2), where a < b

Centres of mass and centroids

### Uppgift 18
Find the centre of mass of an object occupying the cube 0 <= x, y, z <= a with density given by rho = x^2 + y^2 + z^2.

Find the centroids of the regions in Exercises 19–22.

### Uppgift 19
The prism x >= 0, y >= 0, x + y <= 1, 0 <= z <= 1

### Uppgift 20
The unbounded region 0 <= z <= e^(-(x^2+y^2))

### Uppgift 21
The first octant part of the ball x^2 + y^2 + z^2 <= a^2

### Uppgift 22
The region inside the cube 0 <= x, y, z <= 1 and under the plane x + y + z = 2

Moments of inertia

### Uppgift 23
Explain in physical terms why the acceleration of the ball rolling down the incline in Example 4 does not approach g (the acceleration due to gravity) as the angle of incline alpha approaches 90°.

Find the moments of inertia and radii of gyration of the solid objects in Exercises 24–32. Assume constant density in all cases.

### Uppgift 24
A circular cylinder of base radius a and height h about the axis of the cylinder

### Uppgift 25
A circular cylinder of base radius a and height h about a diameter of the base of the cylinder

### Uppgift 26
A right circular cone of base radius a and height h about the axis of the cone

### Uppgift 27
A right circular cone of base radius a and height h about a diameter of the base of the cone

### Uppgift 28
A cube of edge length a about an edge of the cube

### Uppgift 29
A cube of edge length a about a diagonal of a face of the cube

### Uppgift 30
A cube of edge length a about a diagonal of the cube

### Uppgift 31
The rectangular box -a <= x <= a, -b <= y <= b, -c <= z <= c about the z-axis

### Uppgift 32
The region between the two concentric cylinders x^2 + y^2 = a^2 and x^2 + y^2 = b^2 (where 0 < a < b) and between z = 0 and z = c about the z-axis

### Uppgift 33
A ball of radius a has constant density rho. A cylindrical hole of radius b < a is drilled through the centre of the ball. Find the mass of the remaining part of the ball and its moment of inertia about the axis of the hole.

### Uppgift 34
With what acceleration will a solid cylinder of base radius a, height h, and constant density rho roll (without slipping) down a plane inclined at angle alpha to the horizontal?

### Uppgift 35
Repeat Exercise 34 for the ball with the cylindrical hole in Exercise 33. Assume that the axis of the hole remains horizontal while the ball rolls.

### Uppgift 36
A rigid pendulum of mass m swings about point A on a horizontal axis. Its moment of inertia about that axis is I. The centre of mass C of the pendulum is at distance a from A. When the pendulum hangs at rest, C is directly under A. (Why?) Suppose the pendulum is swinging. Let theta = theta(t) measure the angular displacement of the line AC from the vertical at time t. (theta = 0 when the pendulum is in its rest position.) Use a conservation of energy argument similar to that in Example 4 to show that

    (1/2) I (d(theta)/dt)^2 - m g a cos(theta) = constant,

and, hence, differentiating with respect to t, that

    (d^2(theta)/dt^2) + (m g a / I) sin(theta) = 0.

This is a nonlinear differential equation, and it is not easily solved. However, for small oscillations (|theta| small) we can use the approximation sin(theta) ≈ theta. In this case the differential equation is that of simple harmonic motion. What is the period?

### Uppgift 37
Let L_0 be a straight line passing through the centre of mass of a rigid body B of mass m. Let L_k be a straight line parallel to and k units distant from L_0. If I_0 and I_k are the moments of inertia of B about L_0 and L_k, respectively, show that I_k = I_0 + k^2 m. Hence, a body always has smallest moment of inertia about an axis through its centre of mass. Hint: Assume that the z-axis coincides with L_0 and that L_k passes through the point (k, 0, 0).

### Uppgift 38
Reestablish the expression for the total kinetic energy of the rolling ball in Example 4 by regarding the ball at any instant as rotating about a horizontal line through its point of contact with the inclined plane. Use the result of Exercise 37.

### Uppgift 39
(Products of inertia) A rigid body with density rho is placed with its centre of mass at the origin and occupies a region R of 3-space. Suppose the six second moments P_xx, P_yy, P_zz, P_xy, P_xz, and P_yz are all known, where

    P_xx = tripleintegral over R of x^2 rho dV,    P_xy = tripleintegral over R of x y rho dV,   ...

(There exist tables giving these six moments for bodies of many standard shapes. They are called products of inertia.) Show how to express the moment of inertia of the body about any axis through the origin in terms of these six second moments. (If this result is combined with that of Exercise 37, the moment of inertia about any axis can be found.)

[sid 864]

## Chapter Review

Key Ideas

What do the following terms and phrases mean?

- a Riemann sum for f(x, y) on a <= x <= b, c <= y <= d
- f(x, y) is integrable on a <= x <= b, c <= y <= d
- the double integral of f(x, y) over a <= x <= b, c <= y <= d
- iteration of a double integral
- the average value of f(x, y) over region R
- the area element in polar coordinates
- a triple integral
- the volume element in cylindrical coordinates
- the volume element in spherical coordinates
- the surface area of the graph of z = f(x, y)
- the moment of inertia of a solid about an axis

Describe how to change variables in a double integral.

How do you calculate the centroid of a solid region?

How do you calculate the moment of inertia of a solid about an axis?

[sid 865]

Review Exercises

### Oppgift 1
[sid 866]

Evaluate doubleintegral over R of (x + y) dA, over the first-quadrant region lying under x = y^2 and above y = x^2.

### Oppgift 2
Evaluate doubleintegral over P of (x^2 + y^2) dA, where P is the parallelogram with vertices (0,0), (2,0), (3,1), and (1,1).

### Oppgift 3
Find doubleintegral over S of (y/x) dA, where S is the part of the disk x^2 + y^2 <= 4 in the first quadrant and under the line y = x.

### Oppgift 4
Consider the iterated integral

    I = integral from 0 to sqrt(3) of dy integral from sqrt(3)y to sqrt(4-y^2) of e^(-x^2-y^2) dx.

(a) Write I as a double integral doubleintegral over R of e^(-x^2-y^2) dA, and sketch the region R over which the double integral is taken.

(b) Write I as an iterated integral with the order of integrations reversed from that of the given iteration.

(c) Write I as an iterated integral in polar coordinates.

(d) Evaluate I.

### Oppgift 5
Find the constant k > 0 such that the volume of the region lying inside the sphere x^2 + y^2 + z^2 = a^2 and above the cone z = k·sqrt(x^2 + y^2) is one-quarter of the volume contained by the whole sphere.

### Oppgift 6
Reiterate the integral

    I = integral from 0 to 2 of dy integral from 0 to y of f(x,y) dx + integral from 2 to 6 of dy integral from 0 to sqrt(6-y) of f(x,y) dx

with the y integral on the inside.

### Oppgift 7
Let J = integral from 0 to 1 of dz integral from 0 to z of dy integral from 0 to y of f(x,y,z) dx. Express J as an iterated integral where the integrations are to be performed in the following order: first z, then y, then x.

### Oppgift 8
An object in the shape of a right-circular cone has height 10 m and base radius 5 m. Its density is proportional to the square of the distance from the base and equals 3,000 kg/m^3 at the vertex.

(a) Find the mass of the object.

(b) Express the moment of inertia of the object about its central axis as an iterated integral.

### Oppgift 9
Find the average value of f(t) = integral from 0 to t of e^(-x^2) dx over the interval 0 <= t <= a.

### Oppgift 10
Find the average value of the function f(x, y) = floor(x + y) over the quarter-disk x >= 0, y >= 0, x^2 + y^2 <= 4. (Recall that floor(x) denotes the greatest integer less than or equal to x.)

### Oppgift 11
Let D be the smaller of the two solid regions bounded by the surfaces

    z = (x^2 + y^2)/a    and    x^2 + y^2 + z^2 = 6a^2,

where a is a positive constant. Find tripleintegral over D of (x^2 + y^2) dV.

### Oppgift 12
Find the moment of inertia about the z-axis of a solid of density 1 if the solid V is specified by the inequalities 0 <= z <= sqrt(x^2 + y^2) and x^2 + y^2 <= 2ay, where a > 0.

### Oppgift 13
The rectangular solid 0 <= x <= 1, 0 <= y <= 2, 0 <= z <= 1 is cut into two pieces by the plane 2x + y + z = 2. Let D be the piece that includes the origin. Find the volume of D and z-bar, the z-coordinate of the centroid of D.

### Oppgift 14
A solid S consists of those points (x,y,z) that lie in the first octant and satisfy x + y + 2z <= 2 and y + z >= 1. Find the volume of S and the x-coordinate of its centroid.

### Oppgift 15
Find tripleintegral over S of z dV, where S is the portion of the first octant that is above the plane x + y - z = 1 and below the plane z = 1.

### Oppgift 16
Find the area of that part of the plane z = 2x that lies inside the paraboloid z = x^2 + y^2.

[C] ### Oppgift 17
Find the area of that part of the paraboloid z = x^2 + y^2 that lies below the plane z = 2x. Express the answer as a single integral, and evaluate it to 3 decimal places.

[!] ### Oppgift 18
Find the volume of the smaller of the two regions into which the plane x + y + z = 1 divides the interior of the ellipsoid x^2 + 4y^2 + 9z^2 = 36. Hint: First change variables so that the ellipsoid becomes a ball. Then replace the plane by a plane with a simpler equation passing the same distance from the origin.

Challenging Problems

### Challenging 1
The plane (x/a) + (y/b) + (z/c) = 1 (where a > 0, b > 0, and c > 0) divides the solid ellipsoid

    x^2/a^2 + y^2/b^2 + z^2/c^2 <= 1

into two unequal pieces. Find the volume of the smaller piece.

### Challenging 2
Find the area of the part of the plane (x/a) + (y/b) + (z/c) = 1 (where a > 0, b > 0, and c > 0) that lies inside the ellipsoid

    x^2/a^2 + y^2/b^2 + z^2/c^2 <= 1.

### Challenging 3
(a) Expand 1/(1 - xy) as a geometric series, and hence show that

    integral from 0 to 1 of integral from 0 to 1 of 1/(1 - xy) dx dy = sum(n=1 to oo) 1/n^2.

(b) Similarly, express the following integrals as sums of series:

(i) integral from 0 to 1 of integral from 0 to 1 of 1/(1 + xy) dx dy

(ii) integral from 0 to 1 of integral from 0 to 1 of integral from 0 to 1 of 1/(1 - xyz) dx dy dz

(iii) integral from 0 to 1 of integral from 0 to 1 of integral from 0 to 1 of 1/(1 + xyz) dx dy dz

[!] ### Challenging 4
Let P be the parallelepiped bounded by the three pairs of parallel planes a·r = 0, a·r = d_1 > 0, b·r = 0, b·r = d_2 > 0, c·r = 0, and c·r = d_3 > 0, where a, b, and c are constant vectors, and r = x i + y j + z k. Show that

    tripleintegral over P of (a·r)(b·r)(c·r) dx dy dz = (d_1 d_2 d_3)^2 / (8 |a·(b x c)|).

Hint: Make the change of variables u = a·r, v = b·r, w = c·r.

### Challenging 5
A hole whose cross-section is a square of side 2 is punched through the middle of a ball of radius 2. Find the volume of the remaining part of the ball.

[!] ### Challenging 6
Find the volume bounded by the surface with equation x^(2/3) + y^(2/3) + z^(2/3) = a^(2/3).

[!] ### Challenging 7
Find the volume bounded by the surface |x|^(1/3) + |y|^(1/3) + |z|^(1/3) = |a|^(1/3).

