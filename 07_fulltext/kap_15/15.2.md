# Section 15.2: Iteration of Double Integrals in Cartesian Coordinates
> Studieanteckningar baserade på Adams & Essex, Calculus 9th ed, sid 821-828
> Kursens numrering (Adams 10:e uppl.) = 15.2. Motsvarar 14.2 i 9:e uppl., som texten är hämtad ur.

[sid 821]

The existence of the double integral RR_D f(x, y) dA depends on f and the domain D. As we shall see, evaluation of double integrals is easiest when the domain of integration is of simple type.

We say that the domain D in the xy-plane is y-simple if it is bounded by two vertical lines x = a and x = b, and two continuous graphs y = c(x) and y = d(x) between these lines. (See Figure 15.3.) Lines parallel to the y-axis intersect a y-simple domain in an interval (possibly a single point) if at all. Similarly, D is x-simple if it is bounded by horizontal lines y = c and y = d, and two continuous graphs x = a(y) and x = b(y) between these lines. (See Figure 15.4.) Many of the domains over which we will take integrals are y-simple, x-simple, or both. For example, rectangles, triangles, and disks are both x-simple and y-simple. Those domains that are neither one nor the other will usually be unions of finitely many nonoverlapping subdomains that are both x-simple and y-simple. We will call such domains regular.

[FIGUR 15.3: A y-simple domain bounded by x = a, x = b, y = c(x), and y = d(x).]

[FIGUR 15.4: An x-simple domain bounded by y = c, y = d, x = a(y), and x = b(y).]

It can be shown that a bounded, continuous function f(x, y) is integrable over a bounded x-simple or y-simple domain and, therefore, over any regular domain.

Unlike the examples in the previous section, most double integrals cannot be evaluated by inspection. We need a technique for evaluating double integrals similar to the technique for evaluating single definite integrals in terms of antiderivatives. Since the double integral represents a volume, we can evaluate it for simple domains by a slicing technique.

Suppose, for instance, that D is y-simple and is bounded by x = a, x = b, y = c(x), and y = d(x), as shown in Figure 15.5(a). Then RR_D f(x, y) dA represents (at least for positive f) the volume of the solid region inside the vertical cylinder through the boundary of D and between the xy-plane and the surface z = f(x, y). Consider the cross-section of this solid in the vertical plane perpendicular to the x-axis at position x. Note that x is constant in that plane. If we use the projections of the y- and z-axes onto the plane as coordinate axes there, the cross-section is a plane region bounded by vertical lines y = c(x) and y = d(x), by the horizontal line z = 0, and by the curve z = f(x, y). The area of the cross-section is therefore given by

    A(x) = integral(c(x) to d(x)) f(x, y) dy.

The double integral RR_D f(x, y) dA is obtained by summing the volumes of "thin" slices of area A(x) and thickness dx between x = a and x = b and is therefore given by

    RR_D f(x, y) dA = integral(a to b) A(x) dx = integral(a to b) integral(c(x) to d(x)) f(x, y) dy dx.

[FIGUR 15.5(a): In integrals over y-simple domains, slices should be perpendicular to the x-axis.]

Notationally, it is common to omit the large parentheses and write

    RR_D f(x, y) dA = integral(a to b) integral(c(x) to d(x)) f(x, y) dy dx,

or

    RR_D f(x, y) dA = integral(a to b) dx integral(c(x) to d(x)) f(x, y) dy.       (*)

The latter form (*) shows more clearly which variable corresponds to which limits of integration.

[sid 822]

The expressions on the right-hand sides of the above formulas are called iterated integrals. Iteration is the process of reducing the problem of evaluating a double (or multiple) integral to one of evaluating two (or more) successive single definite integrals. In the above iteration, the integral

    integral(c(x) to d(x)) f(x, y) dy

is called the inner integral since it must be evaluated first. It is evaluated using standard techniques, treating x as a constant. The result of this evaluation is a function of x alone (note that both the integrand and the limits of the inner integral can depend on x) and is the integrand of the outer integral in which x is the variable of integration.

For double integrals over x-simple domains, we can slice perpendicularly to the y-axis and obtain an iterated integral with the outer integral in the y direction. (See Figure 15.5(b).) We summarize the above discussion in the following theorem whose formal proof we will, however, not give.

[FIGUR 15.5(b): In integrals over x-simple domains, slices should be perpendicular to the y-axis.]

## THEOREM 1 -- Iteration of double integrals

If f(x, y) is continuous on the bounded y-simple domain D given by a <= x <= b and c(x) <= y <= d(x), then

    RR_D f(x, y) dA = integral(a to b) dx integral(c(x) to d(x)) f(x, y) dy.

Similarly, if f is continuous on the x-simple domain D given by c <= y <= d and a(y) <= x <= b(y), then

    RR_D f(x, y) dA = integral(c to d) dy integral(a(y) to b(y)) f(x, y) dx.

Remark The symbol dA in the double integral is replaced in the iterated integrals by the dx and the dy. Accordingly, dA is frequently written dx dy or dy dx even in the double integral. The three expressions

    RR_D f(x, y) dx dy,   RR_D f(x, y) dy dx,   and   RR_D f(x, y) dA

all stand for the double integral of f over D. Only when the double integral is iterated does the order of dx and dy become important. Later in this chapter we will iterate double integrals in polar coordinates, and dA will take the form r dr dθ.

It is not always necessary to make a three-dimensional sketch of the solid volume represented by a double integral. In order to iterate the integral properly (in one direction or the other) it is usually sufficient to make a sketch of the domain D over which the integral is taken. The direction of iteration can be shown by a line along which the inner integral is taken. The following examples illustrate this.

[sid 823]

## EXAMPLE 1

Find the volume of the solid lying above the square Q defined by 0 <= x <= 1 and 1 <= y <= 2 and below the plane z = 4 - x - y.

Solution The square Q is both x-simple and y-simple, so the double integral giving the volume can be iterated in either direction. We will do it both ways just for practice.

Using one iteration:

    Volume above Q = RR_Q (4 - x - y) dA = integral(1 to 2) dy integral(0 to 1) (4 - x - y) dx
                                         = integral(1 to 2) dy [4x - x^2/2 - xy]_(x=0)^(x=1)
                                         = integral(1 to 2) (7/2 - y) dy
                                         = [7y/2 - y^2/2]_(y=1)^(y=2)
                                         = 2 cubic units.

Using the opposite iteration:

    Volume above Q = RR_Q (4 - x - y) dA = integral(0 to 1) dx integral(1 to 2) (4 - x - y) dy
                                         = integral(0 to 1) dx [4y - xy - y^2/2]_(y=1)^(y=2)
                                         = integral(0 to 1) (5/2 - x) dx
                                         = [5x/2 - x^2/2]_(x=0)^(x=1)
                                         = 2 cubic units.

It is comforting to get the same answer both ways! Note that because Q is a rectangle with sides parallel to the coordinate axes, the limits of the inner integrals do not depend on the variables of the outer integrals in either iteration. This cannot be expected to happen with more general domains.

## EXAMPLE 2

Evaluate RR_T xy dA over the triangle T with vertices (0, 0), (1, 0), and (1, 1).

Solution The triangle T is shown in Figure 15.6. It is both x-simple and y-simple. Using the iteration corresponding to slicing in the direction shown in the figure, we obtain:

    RR_T xy dA = integral(0 to 1) dx integral(0 to x) xy dy
               = integral(0 to 1) dx [xy^2/2]_(y=0)^(y=x)
               = integral(0 to 1) x^4/2 dx
               = [x^5/8]_(x=0)^(x=1)
               = 1/8.

[FIGUR 15.6: The triangular domain T with vertical line indicating iteration with inner integral in the y direction.]

Iteration in the other direction (Figure 15.7) leads to the same value:

    RR_T xy dA = integral(0 to 1) dy integral(y to 1) xy dx
               = integral(0 to 1) dy [x^2 y/2]_(x=y)^(x=1)
               = integral(0 to 1) y(1 - y^2)/2 dy
               = [y^2/4 - y^4/8]_(y=0)^(y=1)
               = 1/8.

[FIGUR 15.7: The triangular domain T with horizontal line indicating iteration with inner integral in the x direction.]

[sid 824]

In both of the examples above, the double integral could be evaluated easily using either possible iteration. (We did them both ways just to illustrate that fact.) It often occurs, however, that a double integral is easily evaluated if iterated in one direction and very difficult, or impossible, if iterated in the other direction. Sometimes you will even encounter iterated integrals whose evaluation requires that they be expressed as double integrals and then reiterated in the opposite direction.

## EXAMPLE 3

Evaluate the iterated integral I = integral(0 to 1) dx integral(x to 1) e^(y^3) dy.

Solution We cannot antidifferentiate e^(y^3) to evaluate the inner integral in this iteration, so we express I as a double integral and identify the region over which it is taken:

    I = RR_D e^(y^3) dA,

where D is the region shown in Figure 15.8. Reiterating with the x integration on the inside we get

    I = integral(0 to 1) dy integral(0 to y^2) e^(y^3) dx
      = integral(0 to 1) e^(y^3) dy integral(0 to y^2) dx
      = integral(0 to 1) y^2 e^(y^3) dy
      = [e^(y^3)/3]_(y=0)^(y=1)
      = (e - 1)/3.

[FIGUR 15.8: The region corresponding to the iterated integral in Example 3.]

The following is an example of the calculation of the volume of a somewhat awkward solid. Even though it is not always necessary to sketch solids to find their volumes, you are encouraged to sketch them whenever possible. When we encounter triple integrals over three-dimensional regions later in this chapter, it will usually be necessary to sketch the regions. Get as much practice as you can.

[sid 825]

## EXAMPLE 4

Sketch and find the volume of the solid bounded by the planes y = 0, z = 0, and z = a - x + y and the parabolic cylinder y = a - (x^2/a), where a is a positive constant.

Solution The solid is shown in Figure 15.9. Its base is the parabolic segment D in the xy-plane bounded by y = 0 and y = a - (x^2/a), so the volume of the solid is given by

    V = RR_D (a - x + y) dA = RR_D (a + y) dA.

(Note how we used symmetry to drop the x term from the integrand. This term is an odd function of x, and D is symmetric about the y-axis.) Iterating the double integral in the direction suggested by the slice shown in the figure, we obtain

    V = integral(-a to a) dx integral(0 to a-(x^2/a)) (a + y) dy
      = integral(-a to a) [ay + y^2/2]_(y=0)^(y=a-(x^2/a)) dx
      = integral(-a to a) [a^2 - x^2 + 1/2(a - 2x^2 + x^4/a^2)] dx
      = integral(-a to a) [3a^2/2 - 2x^2 + x^4/(2a^2)] dx
      = 2 integral(0 to a) [3a^2/2 - 2x^2 + x^4/(2a^2)] dx
      = 2[3a^2 x/2 - 4x^3/3 + x^5/(5a^2)]_(x=0)^(x=a)
      = 2[3a^3/2 - 4a^3/3 + a^3/5]
      = 2[45a^3/30 - 40a^3/30 + 6a^3/30]
      = 28a^3/15 cubic units.

[FIGUR 15.9: The solid in Example 4, sliced perpendicularly to the x-axis.]

[sid 826]

Remark Maple's int routine can be nested to evaluate iterated double (or multiple) integrals symbolically. For instance, the iterated integral for the volume V calculated in Example 4 above can be calculated via the Maple command

    > V = int(int(a+y, y=0..a - x^2/a), x=-a..a);
    V = 28*a^3/15

Recall that "int" has an inert form "Int," which prints the integral without attempting to evaluate it symbolically. For instance, we can print an equation for the reiterated integral in the solution of Example 3 using the command

    > Int(Int(exp(y^3),x=0..y^2),y=0..1) = int(int(exp(y^3),x=0..y^2),y=0..1);
    integral(0 to 1) integral(0 to y^2) e^(y^3) dx dy = (e - 1)/3

If you want Maple to approximate an iterated integral without first trying to evaluate it symbolically, just ask it to evalf the inert form.

    > evalf(Int(Int(exp(y^3),x=0..y^2),y=0..1));
    0.5727606095

[sid 827]

Of course, Maple can't evaluate all integrals in symbolic form. If we replace exp(y^3) in the iterated integral above with exp(x^3), recent versions of Maple will just return the inert form as the answer, being unable to calculate the inner integral.

    > Int(Int(exp(x^3),x=0..y^2),y=0..1) = int(int(exp(x^3),x=0..y^2),y=0..1);
    integral(0 to 1) integral(0 to y^2) e^(x^3) dx dy = integral(0 to 1) integral(0 to y^2) e^(x^3) dx dy

Again, we can force numerical approximation by using evalf on the inert form.

    > Int(Int(exp(x^3),x=0..y^2),y=0..1) = evalf(Int(Int(exp(x^3),x=0..y^2),y=0..1));
    integral(0 to 1) integral(0 to y^2) e^(x^3) dx dy = 0.3668032540

In recent versions of Maple it is not necessary to use the inert form of the integral with evalf, but some earlier versions could produce strange values (e.g., complex numbers for values of evidently real integrals) if you did not use the inert form. Software like Maple is constantly being revised and tweaked so that in unusual circumstances different versions of the software can lead to different results.

[sid 828]

---

## Exercises 15.2

### Uppgift 1
Calculate the given iterated integral: integral(0 to 1) dx integral(0 to x) (xy + y^2) dy.

### Uppgift 2
Calculate the given iterated integral: integral(0 to 1) integral(0 to y) (xy + y^2) dx dy.

### Uppgift 3
Calculate the given iterated integral: integral(0 to π) integral(0 to x) cos y dy dx.

### Uppgift 4
Calculate the given iterated integral: integral(0 to 2) dy integral(0 to y) y^2 e^(xy) dx.

### Uppgift 5
Evaluate the double integral RR_R (x^2 + y^2) dA, where R is the rectangle 0 <= x <= a, 0 <= y <= b.

### Uppgift 6
Evaluate the double integral RR_R x^2 y^2 dA, where R is the rectangle of Exercise 5.

### Uppgift 7
Evaluate the double integral RR_S (sin x + cos y) dA, where S is the square 0 <= x <= π/2, 0 <= y <= π/2.

### Uppgift 8
Evaluate the double integral RR_T (x - 3y) dA, where T is the triangle with vertices (0, 0), (a, 0), and (0, b).

### Uppgift 9
Evaluate the double integral RR_R xy^2 dA, where R is the finite region in the first quadrant bounded by the curves y = x^2 and x = y^2.

### Uppgift 10
Evaluate the double integral RR_D x cos y dA, where D is the finite region in the first quadrant bounded by the coordinate axes and the curve y = 1 - x^2.

### Uppgift 11
Evaluate the double integral RR_D ln x dA, where D is the finite region in the first quadrant bounded by the line 2x + 2y = 5 and the hyperbola xy = 1.

### Uppgift 12
Evaluate the double integral RR_T sqrt(a^2 - y^2) dA, where T is the triangle with vertices (0, 0), (a, 0), and (a, a).

### Uppgift 13
Evaluate the double integral RR_R (x e^y)/y dA, where R is the region 0 <= x <= 1, x^2 <= y <= x.

### Uppgift 14
Evaluate the double integral RR_T (xy)/(1 + x^4) dA, where T is the triangle with vertices (0, 0), (1, 0), and (1, 1).

### Uppgift 15
Sketch the domain of integration and evaluate the given iterated integral: integral(0 to 1) dy integral(y to 1) e^(x^2) dx.

### Uppgift 16
Sketch the domain of integration and evaluate the given iterated integral: integral(0 to π/2) dy integral(y to π/2) (sin x)/x dx.

### Uppgift 17
Sketch the domain of integration and evaluate the given iterated integral: integral(0 to 1) dx integral(x to 1) (y/(x^2 + y^2)) dy (α > 0).

### Uppgift 18
Sketch the domain of integration and evaluate the given iterated integral: integral(0 to 1) dx integral(x to x^(1/3)) sqrt(1 - y^4) dy.

### Uppgift 19
Find the volume under z = 1 - x^2 and above the region 0 <= x <= 1, 0 <= y <= x.

### Uppgift 20
Find the volume under z = 1 - x^2 and above the region 0 <= y <= 1, 0 <= x <= y.

### Uppgift 21
Find the volume under z = 1 - x^2 - y^2 and above the region x >= 0, y >= 0, x + y <= 1.

### Uppgift 22
Find the volume under z = 1 - y^2 and above z = x^2.

### Uppgift 23
Find the volume under the surface z = 1/(x + y) and above the region in the xy-plane bounded by x = 1, x = 2, y = 0, and y = x.

### Uppgift 24
Find the volume under the surface z = x^2 sin(y^4) and above the triangle in the xy-plane with vertices (0, 0), (0, π^(1/4)), and (π^(1/4), π^(1/4)).

### Uppgift 25
Find the volume above the xy-plane and under the surface z = 1 - x^2 - 2y^2.

### Uppgift 26
Find the volume above the triangle with vertices (0, 0), (a, 0), and (0, b), and under the plane z = 2 - (x/a) - (y/b).

### Uppgift 27
Find the volume inside the two cylinders x^2 + y^2 = a^2 and y^2 + z^2 = a^2.

### Uppgift 28
Find the volume inside the cylinder x^2 + 2y^2 = 8, above the plane z = y - 4, and below the plane z = 8 - x.

[A] Uppgift 29
Suppose that f(x, t) and f_1(x, t) are continuous on the rectangle a <= x <= b and c <= t <= d. Let

    g(x) = integral(c to d) f(x, t) dt   and   G(x) = integral(c to d) f_1(x, t) dt.

Show that g'(x) = G(x) for a < x < b. Hint: Evaluate integral(a to x) G(u) du by reversing the order of iteration. Then differentiate the result. This is a different version of Theorem 6 of Section 13.6.

[A] Uppgift 30
Let F'(x) = f(x) and G'(x) = g(x) on the interval a <= x <= b. Let T be the triangle with vertices (a, a), (b, a), and (b, b). By iterating RR_T f(x)g(y) dA in both directions, show that

    integral(a to b) f(x)G(x) dx = F(b)G(b) - F(a)G(a) - integral(a to b) g(y)F(y) dy.

(This is an alternative derivation of the formula for integration by parts.)

[M] Uppgift 31
Use Maple's int routine or similar routines in other computer algebra systems to evaluate the iterated integrals in Exercises 1-4 or the iterated integrals you constructed in the remaining exercises above.

