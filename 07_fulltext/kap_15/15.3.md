# Section 15.3: Improper Integrals and a Mean-Value Theorem
> Studieanteckningar baserade på Adams & Essex, Calculus 9th ed, sid 829-832
> Kursens numrering (Adams 10:e uppl.) = 15.3. Motsvarar 14.3 i 9:e uppl., som texten är hämtad ur.

[sid 829]

To simplify matters, the definition of the double integral given in Section 15.1 required that the domain D be bounded and that the integrand f be bounded on D. As in the single-variable case, improper double integrals can arise if either the domain of integration is unbounded or the integrand is unbounded near any point of the domain or its boundary.

Improper Integrals of Positive Functions

An improper integral of a function f satisfying f(x, y) >= 0 on the domain D must either exist (i.e., converge to a finite value) or be infinite (diverge to infinity). Convergence or divergence of improper double integrals of such nonnegative functions can be determined by iterating them and determining the convergence or divergence of any single improper integrals that result.

## EXAMPLE 1

Evaluate I = RR_R e^(-x^2) dA. Here, R is the region where x >= 0 and -x <= y <= x. (See Figure 15.10.)

Solution We iterate with the outer integral in the x direction:

    I = integral(0 to oo) dx integral(-x to x) e^(-x^2) dy
      = integral(0 to oo) e^(-x^2) dx integral(-x to x) dy
      = 2 integral(0 to oo) x e^(-x^2) dx.

This is an improper integral that can be expressed as a limit:

    I = 2 lim(r -> oo) integral(0 to r) x e^(-x^2) dx
      = 2 lim(r -> oo) [-1/2 e^(-x^2)]_(x=0)^(x=r)
      = lim(r -> oo) (1 - e^(-r^2))
      = 1.

The given integral converges; its value is 1.

[FIGUR 15.10: An unbounded sector of the plane where x >= 0, -x <= y <= x.]

## EXAMPLE 2

If D is the region lying above the x-axis, under the curve y = 1/x, and to the right of the line x = 1, determine whether the double integral

    RR_D dA/(x + y)

converges or diverges.

Solution The region D is sketched in Figure 15.11. We have

    RR_D dA/(x + y) = integral(1 to oo) dx integral(0 to 1/x) dy/(x + y)
                    = integral(1 to oo) [ln(x + y)]_(y=0)^(y=1/x) dx
                    = integral(1 to oo) [ln(x + 1/x) - ln x] dx
                    = integral(1 to oo) ln((x^2 + 1)/x^2) dx
                    = integral(1 to oo) ln(1 + 1/x^2) dx.

It happens that this integral can be evaluated exactly (see Exercise 28 below), but we are only asked to determine whether it converges, and that is more easily accomplished by estimating it. Since 0 < ln(1 + u) < u if u > 0, we have

    0 < RR_D dA/(x + y) < integral(1 to oo) 1/x^2 dx = 1.

Therefore, the given integral converges, and its value lies between 0 and 1.

[FIGUR 15.11: The domain D in Example 2, bounded by x = 1, y = 0, and y = 1/x.]

[sid 830]

## EXAMPLE 3

Evaluate RR_D 1/(x + y)^2 dA, where D is the region 0 <= x <= 1, 0 <= y <= x^2.

Solution The integral is improper because the integrand is unbounded as (x, y) approaches (0, 0), a boundary point of D. (See Figure 15.12.) Nevertheless, iteration leads to a proper integral:

    RR_D 1/(x + y)^2 dA = lim(c -> 0+) integral(c to 1) dx integral(0 to x^2) 1/(x + y)^2 dy
                        = lim(c -> 0+) integral(c to 1) dx [-1/(x + y)]_(y=0)^(y=x^2)
                        = lim(c -> 0+) integral(c to 1) [1/x - 1/(x + x^2)] dx
                        = integral(0 to 1) 1/(x + 1) dx
                        = [ln(x + 1)]_(x=0)^(x=1)
                        = ln 2.

[FIGUR 15.12: The function 1/(x + y)^2 is unbounded on D.]

[sid 831]

## EXAMPLE 4

Determine the convergence or divergence of I = RR_D dA/(xy), where D is the bounded region in the first quadrant lying between the line y = x and the parabola y = x^2.

Solution The domain D is shown in Figure 15.13. Again, the integral is improper because the integrand 1/(xy) is unbounded as (x, y) approaches the boundary point (0, 0). We have

    I = integral(0 to 1) dx integral(x^2 to x) dy/(xy)
      = integral(0 to 1) (1/x) [ln y]_(y=x^2)^(y=x) dx
      = integral(0 to 1) (1/x) (ln x - ln x^2) dx
      = integral(0 to 1) (1/x) (-ln x) dx
      = -integral(0 to 1) (ln x)/x dx.

If we substitute x = e^(-t) in this integral, we obtain

    I = -integral(0 to oo) (-t)/(-e^(-t)) e^(-t) dt = integral(0 to oo) t dt,

which diverges to infinity.

[FIGUR 15.13: The function 1/(xy) is unbounded on the domain D.]

Remark In each of the examples above, the integrand was nonnegative on the domain of integration. Nonpositive integrands could have been handled similarly, but we cannot deal here with the convergence of general improper double integrals with integrands f(x, y) that take both positive and negative values on the domain D of the integral. We remark, however, that such an integral cannot converge unless

    RR_E f(x, y) dA

is finite for every bounded, regular subdomain E of D. We cannot, in general, determine the convergence of the given integral by looking at the convergence of iterations. The double integral may diverge even if its iterations converge. (See Exercise 21 below.) In fact, opposite iterations may even give different values. This happens because of cancellation of infinite volumes of opposite sign. (Similar behaviour in one dimension is exemplified by the integral integral(-oo to oo) dx/x, which does not exist, although it represents the difference between "equal" but infinite areas.) It can be shown (for a large class of functions containing, for example, continuous functions) that an improper double integral of f(x, y) over D converges if the integral of |f(x, y)| over D converges:

    RR_D |f(x, y)| dA converges => RR_D f(x, y) dA converges.

In this case any iterations will converge to the same value. Such double integrals are called absolutely convergent by analogy with absolutely convergent infinite series.

A Mean-Value Theorem for Double Integrals

Let D be a set in the xy-plane that is closed and bounded and has positive area A = RR_D dA. Suppose that f(x, y) is continuous on D. Then there exist points (x_1, y_1) and (x_2, y_2) in D where f assumes minimum and maximum values (see Theorem 2 of Section 13.1); that is,

    f(x_1, y_1) <= f(x, y) <= f(x_2, y_2)

for all points (x, y) in D. If we integrate this inequality over D, we obtain

    f(x_1, y_1) A = RR_D f(x_1, y_1) dA <= RR_D f(x, y) dA <= RR_D f(x_2, y_2) dA = f(x_2, y_2) A.

Therefore, dividing by A, we find that the number

    f-bar = (1/A) RR_D f(x, y) dA

lies between the minimum and maximum values of f on D:

    f(x_1, y_1) <= f-bar <= f(x_2, y_2).

A set D in the plane is said to be connected if any two points in it can be joined by a continuous parametric curve x = x(t), y = y(t), (0 <= t <= 1), lying in D. Suppose this curve joins (x_1, y_1) (where t = 0) and (x_2, y_2) (where t = 1). Let g(t) satisfy

    g(t) = f(x(t), y(t)),   0 <= t <= 1.

Then g is continuous and takes the values f(x_1, y_1) at t = 0 and f(x_2, y_2) at t = 1. By the Intermediate-Value Theorem there exists a number t_0 between 0 and 1 such that

    f-bar = g(t_0) = f(x_0, y_0),

where x_0 = x(t_0) and y_0 = y(t_0). Thus, we have found a point (x_0, y_0) in D such that

    (1/(area of D)) RR_D f(x, y) dA = f(x_0, y_0).

We have therefore proved the following version of the Mean-Value Theorem.

## THEOREM 1 -- A Mean-Value Theorem for double integrals

If the function f(x, y) is continuous on a closed, bounded, connected set D in the xy-plane, then there exists a point (x_0, y_0) in D such that

    RR_D f(x, y) dA = f(x_0, y_0) * (area of D).

By analogy with the definition of average value for one-variable functions, we make the following definition:

## DEFINITION 1 -- Average value of a function

The average value or mean value of an integrable function f(x, y) over the set D is the number

    f-bar = (1/(area of D)) RR_D f(x, y) dA.

If f(x, y) >= 0 on D, then the cylinder with base D and constant height f-bar has volume equal to that of the solid region lying above D and below the surface z = f(x, y). It is often very useful to interpret a double integral in terms of the average value of the function which is its integrand.

## EXAMPLE 5

The average value of x over a domain D having area A is

    x-bar = (1/A) RR_D x dA.

Of course, x-bar is just the x-coordinate of the centroid of the region D.

[sid 832]

## EXAMPLE 6

A large number of points (x, y) are chosen at random in the triangle T with vertices (0, 0), (1, 0), and (1, 1). What is the approximate average value of x^2 + y^2 for these points?

Solution The approximate average value of x^2 + y^2 for the randomly chosen points will be the average value of that function over the triangle, namely,

    (1/(1/2)) RR_T (x^2 + y^2) dA = 2 integral(0 to 1) dx integral(0 to x) (x^2 + y^2) dy
                                  = 2 integral(0 to 1) [x^2 y + y^3/3]_(y=0)^(y=x) dx
                                  = 2 integral(0 to 1) (x^3 + x^3/3) dx
                                  = 2 integral(0 to 1) (4x^3/3) dx
                                  = (8/3) [x^4/4]_(x=0)^(x=1)
                                  = 2/3.

## EXAMPLE 7

Let (a, b) be an interior point of a domain D on which f(x, y) is continuous. For sufficiently small positive r, the closed circular disk D_r with centre at (a, b) and radius r is contained in D. Show that

    lim(r -> 0) (1/(π r^2)) RR_(D_r) f(x, y) dA = f(a, b).

Solution If D_r is contained in D, then by Theorem 1

    (1/(π r^2)) RR_(D_r) f(x, y) dA = f(x_0, y_0)

for some point (x_0, y_0) in D_r. As r -> 0, the point (x_0, y_0) approaches (a, b). Since f is continuous at (a, b), we have f(x_0, y_0) -> f(a, b). Thus,

    lim(r -> 0) (1/(π r^2)) RR_(D_r) f(x, y) dA = f(a, b).

---

## Exercises 15.3

### Uppgift 1
In Exercises 1-12, determine whether the given integral converges or diverges. Try to evaluate those that converge.

Evaluate RR_Q e^(-x-y) dA, where Q is the first quadrant of the xy-plane.

### Uppgift 2
Evaluate RR_Q dA/((1 + x^2)(1 + y^2)), where Q is the first quadrant of the xy-plane.

### Uppgift 3
Evaluate RR_S y/(1 + x^2) dA, where S is the strip 0 < y < 1 in the xy-plane.

### Uppgift 4
Evaluate integral over T 1/sqrt(xy) dA over the triangle T with vertices (0, 0), (1, 1), and (1, 2).

### Uppgift 5
Evaluate RR_Q (x^2 + y^2)/((1 + x^2)(1 + y^2)) dA, where Q is the first quadrant of the xy-plane.

### Uppgift 6
Evaluate RR_H 1/(1 + x + y) dA, where H is the half-strip 0 <= x < oo, 0 < y < 1.

### Uppgift 7
Determine whether integral over R 2 e^(-(|x| + |y|)) dA converges or diverges.

### Uppgift 8
Determine whether RR_R 2 e^(-|x + y|) dA converges or diverges.

### Uppgift 9
Determine whether RR_T e^(-y/x^3) dA converges or diverges, where T is the region satisfying x >= 1 and 0 <= y <= x.

### Uppgift 10
Determine whether RR_T dA/(x^2 + y^2) converges or diverges, where T is the region in Exercise 9.

### Uppgift 11
[!] Determine whether RR_Q e^(-xy) dA converges or diverges, where Q is the first quadrant of the xy-plane.

### Uppgift 12
Determine whether RR_R 1/(x^(1/2) sin(1/x^(1/2))) dA converges or diverges, where R is the region 2/π <= x < 1, 0 <= y <= 1/x.

### Uppgift 13
Evaluate

    I = RR_S dA/(x + y),

where S is the square 0 <= x <= 1, 0 <= y <= 1,

(a) by direct iteration of the double integral,

(b) by using the symmetry of the integrand and the domain to write

    I = 2 RR_T dA/(x + y),

where T is the triangle with vertices (0, 0), (1, 0), and (1, 1).

### Uppgift 14
Find the volume of the solid lying above the square S of Exercise 13 and under the surface z = 2xy/(x^2 + y^2).

### Uppgift 15
Find all real values of k for which RR_(D_k) dA/x^a converges, where D_k is the region 0 <= x <= 1, 0 <= y <= x^k.

### Uppgift 16
Find all real values of k for which RR_(D_k) y^b dA converges, where D_k is the region 0 <= x <= 1, 0 <= y <= x^k.

### Uppgift 17
Find all real values of k for which RR_(R_k) x^a dA converges, where R_k is the region 1 <= x < oo, 0 <= y <= x^k.

### Uppgift 18
Find all real values of k for which RR_(R_k) dA/y^b converges, where R_k is the region 1 <= x < oo, 0 <= y <= x^k.

### Uppgift 19
Find all real values of k for which RR_(D_k) x^a y^b dA converges, where D_k is the region 0 <= x <= 1, 0 <= y <= x^k.

### Uppgift 20
Find all real values of k for which RR_(R_k) x^a y^b dA converges, where R_k is the region 1 <= x < oo, 0 <= y <= x^k.

### Uppgift 21
[!] Evaluate both iterations of the improper integral

    RR_S (x - y)/(x + y)^3 dA,

where S is the square 0 < x < 1, 0 < y < 1. Show that the above improper double integral does not exist by considering

    RR_T (x - y)/(x + y)^3 dA,

where T is that part of the square S lying under the line x = y.

### Uppgift 22
Find the average value of x^2 over the rectangle a <= x <= b, c <= y <= d.

### Uppgift 23
Find the average value of x^2 + y^2 over the triangle 0 <= x <= a, 0 <= y <= a - x.

### Uppgift 24
Find the average value of 1/x over the region 0 <= x <= 1, x^2 <= y <= x.

### Uppgift 25
Find the average distance from points in the quarter-disk x^2 + y^2 <= a^2, x >= 0, y >= 0, to the line x + y = 0.

### Uppgift 26
Does f(x, y) = x have an average value over the region 0 <= x < oo, 0 <= y <= 1/(1 + x^2)? If so, what is it?

### Uppgift 27
Does f(x, y) = xy have an average value over the region 0 <= x < oo, 0 <= y <= 1/(1 + x^2)? If so, what is it?

[A] Uppgift 28
Find the exact value of the integral in Example 2. Hint: Integrate by parts in integral(1 to oo) ln(1 + (1/x^2)) dx.

[A] Uppgift 29
Let (a, b) be an interior point of a domain D on which the function f(x, y) is continuous. For small enough h^2 + k^2 the rectangle R_hk with vertices (a, b), (a + h, b), (a, b + k), and (a + h, b + k) is contained in D. Show that

    lim((h,k) -> (0,0)) (1/(hk)) RR_(R_hk) f(x, y) dA = f(a, b).

Hint: See Example 7.

[A] Uppgift 30
(Another proof of equality of mixed partials) Suppose that f_12(x, y) and f_21(x, y) are continuous in a neighbourhood of the point (a, b). Without assuming the equality of these mixed partial derivatives, show that

    RR_R f_12(x, y) dA = RR_R f_21(x, y) dA,

where R is the rectangle with vertices (a, b), (a + h, b), (a, b + k), and (a + h, b + k) and h^2 + k^2 is sufficiently small. Now use the result of Exercise 29 to show that

    f_12(a, b) = f_21(a, b).

(This reproves Theorem 1 of Section 12.4. However, in that theorem we only assumed continuity of the mixed partials at (a, b). Here, we assume the continuity at all points sufficiently near (a, b).)

