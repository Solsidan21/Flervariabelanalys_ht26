# Section 15.4: Double Integrals in Polar Coordinates
> Studieanteckningar baserade på Adams & Essex, Calculus 9th ed, sid 833-842
> Kursens numrering (Adams 10:e uppl.) = 15.4. Motsvarar 14.4 i 9:e uppl., som texten är hämtad ur.

[sid 833]

## Motivation and Setup

For many applications of double integrals, either the domain of integration, the integrand function, or both may be more easily expressed in terms of polar coordinates than in terms of Cartesian coordinates. Recall that a point P with Cartesian coordinates (x, y) can also be located by its polar coordinates [r, theta], where r is the distance from P to the origin O, and theta is the angle OP makes with the positive direction of the x-axis. (Positive angles theta are measured counterclockwise.) The polar and Cartesian coordinates of P are related by the transformations:

    x = r cos(theta),           r^2 = x^2 + y^2,
    y = r sin(theta),           tan(theta) = y/x.

Consider the problem of finding the volume V of the solid region lying above the xy-plane and beneath the paraboloid z = 1 - x^2 - y^2. Since the paraboloid intersects the xy-plane in the circle x^2 + y^2 = 1, the volume is given in Cartesian coordinates by

    V = doubleintegral over {x^2+y^2 <= 1} of (1 - x^2 - y^2) dA
      = integral(x=-1 to 1) dx integral(y=-sqrt(1-x^2) to sqrt(1-x^2)) (1 - x^2 - y^2) dy.

Evaluating this iterated integral would require considerable effort. However, we can express the same volume in terms of polar coordinates as

    V = doubleintegral over {r<=1} of (1 - r^2) dA.

In order to iterate this integral, we have to know the form that the area element dA takes in polar coordinates.

[FIGUR 15.4: (a) dA = dx dy in Cartesian coordinates; (b) dA = r dr dtheta in polar coordinates]

In the Cartesian formula for V, the area element dA = dx dy represents the area of the "infinitesimal" region bounded by the coordinate lines at x, x + dx, y, and y + dy. In the polar formula, the area element dA should represent the area of the "infinitesimal" region bounded by the coordinate circles with radii r and r + dr, and coordinate rays from the origin at angles theta and theta + dtheta. Observe that dA is approximately the area of a rectangle with dimensions dr and r dtheta. The error in this approximation becomes negligible compared with the size of dA as dr and dtheta approach zero. Thus, in transforming a double integral between Cartesian and polar coordinates, the area element transforms according to the formula

    dx dy = dA = r dr dtheta.

In order to iterate the polar form of the double integral for V considered above, we can regard the domain of integration as a set in a plane having Cartesian coordinates r and theta. In the xy Cartesian plane the domain is a disk r <= 1, but in the r theta-plane (with perpendicular r- and theta-axes) the domain is the rectangle R specified by 0 <= r <= 1 and 0 <= theta <= 2*pi. The area element in the r-theta-plane is dA = dr dtheta, so area is not preserved under the transformation to polar coordinates (dA = r dA_r-theta). Thus, the polar integral for V is really a Cartesian integral in the r-theta-plane, with integrand modified by the inclusion of an extra factor r to compensate for the change of area. It can be evaluated by standard iteration methods:

    V = doubleintegral over R of (1 - r^2) r dA = integral(theta=0 to 2*pi) dtheta integral(r=0 to 1) (1 - r^2) r dr
      = integral(theta=0 to 2*pi) [r^2/2 - r^4/4]_{r=0}^{r=1} dtheta
      = integral(theta=0 to 2*pi) [1/2 - 1/4] dtheta = integral(theta=0 to 2*pi) 1/4 dtheta
      = pi/2 cubic units.

**Remark** It is not necessary to sketch the region R in the r-theta-plane. We are used to thinking of polar coordinates in terms of distances and angles in the xy-plane and can easily understand from looking at the disk that the iteration of the integral in polar coordinates corresponds to 0 <= theta <= 2*pi and 0 <= r <= 1. That is, we should be able to write the iteration

    V = integral(theta=0 to 2*pi) dtheta integral(r=0 to 1) (1 - r^2) r dr

directly from consideration of the domain of integration in the xy-plane.

[sid 834]

[sid 835]

## EXAMPLE 1

If R is that part of the annulus 0 < a^2 <= x^2 + y^2 <= b^2 lying in the first quadrant and below the line y = x, evaluate

    I = doubleintegral over R of (y^2/x^2) dA.

**Solution** The region R is specified in polar coordinates by 0 <= theta <= pi/4 and a <= r <= b. Since

    y^2/x^2 = r^2 sin^2(theta) / (r^2 cos^2(theta)) = tan^2(theta),

we have

    I = integral(theta=0 to pi/4) tan^2(theta) dtheta integral(r=a to b) r dr
      = (1/2)(b^2 - a^2) integral(theta=0 to pi/4) (sec^2(theta) - 1) dtheta
      = (1/2)(b^2 - a^2) [tan(theta) - theta]_{theta=0}^{theta=pi/4}
      = (1/2)(b^2 - a^2) [1 - pi/4]
      = (pi/8)(b^2 - a^2).

## EXAMPLE 2

**(Area of a polar region)** Derive the formula for the area of the polar region R bounded by the curve r = f(theta) and the rays theta = alpha and theta = beta.

**Solution** The area A of R is numerically equal to the volume of a cylinder of height 1 above the region R:

    A = doubleintegral over R of dx dy = doubleintegral over R of r dr dtheta
      = integral(theta=alpha to beta) dtheta integral(r=0 to f(theta)) r dr
      = (1/2) integral(theta=alpha to beta) f(theta)^2 dtheta.

Observe that the inner integral in the iteration involves integrating r along the ray specified by theta from 0 to f(theta).

There is no firm rule as to whether one should or should not convert a double integral from Cartesian to polar coordinates. In Example 1 above, the conversion was strongly suggested by the shape of the domain but was also indicated by the fact that the integrand, y^2/x^2, becomes a function of theta alone when converted to polar coordinates. It is usually wise to switch to polar coordinates if the switch simplifies the iteration (i.e., if the domain is "simpler" when expressed in terms of polar coordinates), even if the form of the integrand is made more complicated.

## EXAMPLE 3

Find the volume of the solid lying in the first octant, inside the cylinder x^2 + y^2 = a^2, and under the plane z = y.

**Solution** The solid has its base as a quarter disk, which is expressed in polar coordinates by the inequalities 0 <= theta <= pi/2 and 0 <= r <= a. The height is given by z = y = r sin(theta). The solid has volume

    V = integral(theta=0 to pi/2) dtheta integral(r=0 to a) (r sin(theta)) r dr
      = integral(theta=0 to pi/2) sin(theta) dtheta integral(r=0 to a) r^2 dr
      = 1 * (1/3) a^3 = a^3/3 cubic units.

[sid 836]

The following example establishes the value of a definite integral that plays a very important role in probability theory and statistics. It is interesting that this single-variable integral cannot be evaluated by the techniques of single-variable calculus.

## EXAMPLE 4

**(A Very Important Integral)** Show that

    integral(x=-oo to oo) e^(-x^2) dx = sqrt(pi).

**Solution** The improper integral (call it I) converges, and its value does not depend on what symbol we use for the variable of integration. Therefore, we can express the square of the integral as a product of two identical integrals but with their variables of integration named differently. We then interpret this product as an improper double integral and reiterate it in polar coordinates:

    I^2 = (integral(x=-oo to oo) e^(-x^2) dx)^2 * (integral(y=-oo to oo) e^(-y^2) dy)
        = doubleintegral over R^2 of e^(-(x^2+y^2)) dA
        = integral(theta=0 to 2*pi) dtheta integral(r=0 to oo) e^(-r^2) r dr
        = 2*pi * lim(R->oo) [-1/2 * e^(-r^2)]_{r=0}^{r=R} = pi.

Thus, I = sqrt(pi), as asserted.

Note that the r integral in the iteration above is a convergent improper integral; it was evaluated with the aid of the substitution u = r^2.

As our final example of iteration in polar coordinates, let us try something a little more demanding.

## EXAMPLE 5

Find the volume of the solid region lying inside both the sphere x^2 + y^2 + z^2 = 4a^2 and the cylinder x^2 + y^2 = 2ay, where a > 0.

**Solution** The sphere is centred at the origin and has radius 2a. The equation of the cylinder becomes

    x^2 + (y - a)^2 = a^2

if we complete the square in the y terms. Thus, it is a vertical circular cylinder of radius a having its axis along the vertical line through (0, a, 0). The z-axis lies on the cylinder. One-quarter of the required volume lies in the first octant.

If we use polar coordinates in the xy-plane, then the sphere has equation

    r^2 + z^2 = 4a^2

and the cylinder has equation r^2 = 2ar sin(theta) or, more simply,

    r = 2a sin(theta).

The first octant portion of the volume lies above the region specified by the inequalities 0 <= theta <= pi/2 and 0 <= r <= 2a sin(theta). Therefore, the total volume is

    V = 4 * integral(theta=0 to pi/2) dtheta integral(r=0 to 2a*sin(theta)) sqrt(4a^2 - r^2) r dr
      = 2 * integral(theta=0 to pi/2) dtheta integral(u=4a^2 to 4a^2*cos^2(theta)) sqrt(u) du    [Let u = 4a^2 - r^2]
      = (4/3) * integral(theta=0 to pi/2) (8a^3 - 8a^3*cos^3(theta)) dtheta    [Let v = sin(theta)]
      = (16/3)*a^3 - (32/3)*a^3 * integral(v=0 to 1) (1 - v^2) dv
      = (16/3)*a^3 - (64/9)*a^3
      = (16/9)(3 - 4)*a^3 cubic units.

[sid 837]

## Change of Variables in Double Integrals

The transformation of a double integral to polar coordinates is just a special case of a general change of variables formula for double integrals. Suppose that x and y are expressed as functions of two other variables u and v by the equations

    x = x(u, v)
    y = y(u, v).

We regard these equations as defining a transformation (or mapping) from points (u, v) in a uv-Cartesian plane to points (x, y) in the xy-plane. We say that the transformation is one-to-one from the set S in the uv-plane onto the set D in the xy-plane provided:

(i) every point in S gets mapped to a point in D,
(ii) every point in D is the image of a point in S, and
(iii) different points in S get mapped to different points in D.

If the transformation is one-to-one, the defining equations can be solved for u and v as functions of x and y, and the resulting inverse transformation,

    u = u(x, y)
    v = v(x, y),

is one-to-one from D onto S.

Let us assume that the functions x(u, v) and y(u, v) have continuous first partial derivatives and that the Jacobian determinant

    partial(x,y)/partial(u,v) != 0    at (u, v).

As noted in Section 12.8, the Implicit Function Theorem implies that the transformation is one-to-one near (u, v) and the inverse transformation also has continuous first partial derivatives and nonzero Jacobian satisfying

    partial(u,v)/partial(x,y) = 1 / (partial(x,y)/partial(u,v))    on D.

The transformation x = r cos(theta), y = r sin(theta) to polar coordinates has Jacobian

    partial(x,y)/partial(r,theta) = |cos(theta)  -r sin(theta)|
                                     |sin(theta)   r cos(theta)| = r.

Near any point except the origin (where r = 0) the transformation is one-to-one. (In fact, it is one-to-one from any set in the r-theta-plane that does not contain more than one point where r = 0 and lies in, say, the strip 0 <= theta < 2*pi.)

A one-to-one transformation can be used to transform the double integral

    doubleintegral over D of f(x, y) dA

to a double integral over the corresponding set S in the uv-plane. Under the transformation, the integrand f(x, y) becomes g(u, v) = f(x(u, v), y(u, v)). We must discover how to express the area element dA = dx dy in terms of the area element du dv in the uv-plane.

If the value of u is fixed, say u = c, the equations

    x = x(c, v) and      y = y(c, v)

define a parametric curve (with v as parameter) in the xy-plane. This curve is called a u-curve corresponding to the value u = c. Similarly, for fixed v = c the equations

    x = x(u, c) and       y = y(u, c)

define a parametric curve (with parameter u) called a v-curve. Consider the differential area element bounded by the u-curves corresponding to nearby values u and u + du and the v-curves corresponding to nearby values v and v + dv. Since these curves are smooth, for small values of du and dv the area element is approximately a parallelogram, and its area is approximately

    dA = |PQ x PR|,

where P, Q, and R are points shown in the figure. The error in this approximation becomes negligible compared with dA as du and dv approach zero.

Now PQ = dx i + dy j, where

    dx = (partial x/partial u) du + (partial x/partial v) dv    and   dy = (partial y/partial u) du + (partial y/partial v) dv.

However, dv = 0 along the v-curve PQ, so

    PQ = (partial x/partial u) du i + (partial y/partial u) du j.

Similarly,

    PR = (partial x/partial v) dv i + (partial y/partial v) dv j.

Hence,

    dA = |i              j              k       |
         |partial x/partial u*du  partial y/partial u*du  0|
         |partial x/partial v*dv  partial y/partial v*dv  0|
       = |partial(x,y)/partial(u,v)| du dv,

that is, the absolute value of the Jacobian partial(x,y)/partial(u,v) is the ratio between corresponding area elements in the xy-plane and the uv-plane:

    dA = dx dy = |partial(x,y)/partial(u,v)| du dv.

The following theorem summarizes the change of variables procedure for a double integral.

[sid 838]

## THEOREM 4 -- Change of variables formula for double integrals

Let x = x(u, v), y = y(u, v) be a one-to-one transformation from a domain S in the uv-plane onto a domain D in the xy-plane. Suppose that the functions x and y, and their first partial derivatives with respect to u and v, are continuous in S. If f(x, y) is integrable on D, and if g(u, v) = f(x(u, v), y(u, v)), then g is integrable on S and

    doubleintegral over D of f(x, y) dx dy = doubleintegral over S of g(u, v) |partial(x,y)/partial(u,v)| du dv.

**Remark** It is not necessary that S or D be closed or that the transformation be one-to-one on the boundary of S. The transformation to polar coordinates maps the rectangle 0 < r < 1, 0 <= theta < 2*pi one-to-one onto the punctured disk 0 < x^2 + y^2 < 1 and, as in the first example in this section, we can transform an integral over the closed disk x^2 + y^2 <= 1 to one over the closed rectangle 0 <= r <= 1, 0 <= theta <= 2*pi.

## EXAMPLE 6

Use an appropriate change of variables to find the area of the elliptic disk E given by

    x^2/a^2 + y^2/b^2 <= 1.

**Solution** Under the transformation x = au, y = bv, the elliptic disk E is the one-to-one image of the circular disk D given by u^2 + v^2 <= 1. Assuming a > 0 and b > 0, we have

    dx dy = |partial(x,y)/partial(u,v)| du dv = |a  0| du dv = ab du dv.
                                                  |0  b|

Therefore, the area of E is given by

    doubleintegral over E of 1 dx dy = doubleintegral over D of ab du dv = ab * (area of D) = ab*pi square units.

[sid 839]

It is often tempting to try to use the change of variable formula to transform the domain of a double integral into a rectangle so that iteration will be easy. As the following example shows, this usually involves defining the inverse transformation (u and v in terms of x and y). Remember that inverse transformations have reciprocal Jacobians.

## EXAMPLE 7

Find the area of the finite plane region bounded by the four parabolas y = x^2, y = 2x^2, x = y^2, and x = 3y^2.

**Solution** The region, call it D, is sketched in Figure 14.34. Let

    u = x^2/y    and    v = y^2/x.

Then the region D corresponds to the rectangle R in the uv-plane given by 1/2 <= u <= 1 and 1/3 <= v <= 1. Since

    partial(u,v)/partial(x,y) = |2x/y   -x^2/y^2|
                                |-y^2/x^2  2y/x  | = 4 - 1 = 3,

we have

    |partial(x,y)/partial(u,v)| = 1/3

and so the area of D is given by

    doubleintegral over D of dx dy = doubleintegral over R of (1/3) du dv = (1/3) * (1/2) * (2/3) = 1/9 square units.

[sid 840]

## EXAMPLE 8

Evaluate

    I = doubleintegral over D of (y/x) dx dy,

where D is the shaded region shown in the figure.

**Solution** We use the change of variables u = x^2 + 4y^2, v = y/x, so that the region D in the uv-plane that corresponds to D is the rectangle 0 <= u <= 4, 0 <= v <= 1. Since

    partial(u,v)/partial(x,y) = |2x       8y    |
                                |-y/x^2   1/x   | = 2 + 8*y^2/x^2 = 2 + 8v^2,

we have

    partial(x,y)/partial(u,v) = 1/(2 + 8v^2),

and so

    I = doubleintegral over R of [v/(2 + 8v^2)] du dv = integral(0 to 4) du integral(0 to 1) [v/(2 + 8v^2)] dv

Using the substitution w = 2 + 8v^2, dw = 16v dv:

    I = (1/16) * ln(w)|_{w=2}^{w=10} = (1/16) * (ln 10 - ln 2) = (1/4) * ln 5.

Wait, let me recalculate. We have:

    I = integral(0 to 4) du * integral(0 to 1) [v/(2 + 8v^2)] dv
      = 4 * integral(0 to 1) [v/(2 + 8v^2)] dv
      = 4 * [1/16 * ln(2 + 8v^2)]_{v=0}^{v=1}
      = 4 * (1/16) * [ln(10) - ln(2)]
      = (1/4) * ln(5).

[sid 841]

## EXAMPLE 9

Evaluate

    I = doubleintegral over T of (x + y)^3 dx dy

over the triangle T with vertices (0, 0), (1, 1), and (2, 0).

**Solution** The triangle is shown in the figure. The transformation u = y - x, v = y + x is linear, so its image in the uv-plane is also a triangle R, this one with vertices (0, 0), (0, 2), and (-2, 2). Since

    partial(u,v)/partial(x,y) = |-1   1|
                                | 1   1| = -2,

we have

    |partial(x,y)/partial(u,v)| = 1/2

and we can calculate I as

    I = (1/2) * doubleintegral over R of v^3 du dv
      = (1/2) * integral(0 to 2) v^3 dv * integral(0 to -v) du
      = (1/2) * [v^4/4]_{v=0}^{v=2} * (-v)
      
Wait, let me recalculate more carefully. The vertices map as follows:
- (0, 0) -> (0, 0)
- (1, 1) -> (0, 2)
- (2, 0) -> (-2, 2)

So the region R has bounds: for v from 0 to 2, and u from -v to 0, or equivalently from -v to v but restricted...

Actually, looking at the region more carefully: for a given v in [0, 2], u ranges from the line from (0,0) to (-2,2), which is u = -v, to the line from (0,0) to (0,2), which is u = 0.

So:
    I = (1/2) * integral(0 to 2) v^3 dv integral(-v to 0) du
      = (1/2) * integral(0 to 2) v^3 * v dv
      = (1/2) * integral(0 to 2) v^4 dv
      = (1/2) * [v^5/5]_{v=0}^{v=2}
      = (1/2) * (32/5)
      = 16/5.

[sid 842]

## EXAMPLE 10

Let D be the square 0 <= x <= 1, 0 <= y <= 1 in the xy-plane, and let S be the square 0 <= u <= 1, 0 <= v <= 1 in the uv-plane. Show that the transformation

    x = 4u - 4u^2,    y = v

maps S onto D, and use it to transform the integral

    I = doubleintegral over D of dx dy.

Compare the value of I with that of the transformed integral.

**Solution** Since x = 4u - 4u^2 = 1 - (1 - 2u)^2, the minimum value of x on the interval 0 <= u <= 1 is 0 (at u = 0 and u = 1), and the maximum value is 1 (at u = 1/2). Therefore, x = 4u - 4u^2 maps the interval 0 <= u <= 1 onto the interval 0 <= x <= 1. Since y = v clearly maps 0 <= v <= 1 onto 0 <= y <= 1, the given transformation maps S onto D. Since

    dx dy = |partial(x,y)/partial(u,v)| du dv = |4 - 8u  0| du dv = |4 - 8u| du dv,
                                                  |0       1|

transforming I leads to the integral

    J = doubleintegral over S of |4 - 8u| du dv = 4 * integral(0 to 1) dv * integral(0 to 1) |1 - 2u| du
      = 8 * integral(0 to 1/2) (1 - 2u) du = 8 * [u - u^2]_{u=0}^{u=1/2} = 8 * (1/2 - 1/4) = 2.

However, I = doubleintegral over D of dx dy = area of D = 1. The reason that J != I is that the transformation is not one-to-one from S onto D; it actually maps S onto D twice. The rectangle R defined by 0 <= u <= 1/2 and 0 <= v <= 1 (i.e., the left half of S), is mapped one-to-one onto D by the transformation, so the appropriate transformed integral is

    doubleintegral over R of |4 - 8u| du dv,

which is equal to I.

## Exercises 15.4

### Uppgift 1
Evaluate doubleintegral over D of (x^2 + y^2) dA, where D is given by x^2 + y^2 <= a^2, where a > 0.

### Uppgift 2
Evaluate doubleintegral over D of sqrt(x^2 + y^2) dA, where D is the disk x^2 + y^2 <= a^2, where a > 0.

### Uppgift 3
Evaluate doubleintegral over D of (1/sqrt(x^2 + y^2)) dA, where D is the disk x^2 + y^2 <= a^2, where a > 0.

### Uppgift 4
Evaluate doubleintegral over D of |x| dA, where D is the disk x^2 + y^2 <= a^2, where a > 0.

### Uppgift 5
Evaluate doubleintegral over D of x^2 dA, where D is the disk x^2 + y^2 <= a^2, where a > 0.

### Uppgift 6
Evaluate doubleintegral over D of x^2*y^2 dA, where D is the disk x^2 + y^2 <= a^2, where a > 0.

### Uppgift 7
Evaluate doubleintegral over Q of y dA, where Q is the quarter-disk x >= 0, y >= 0, x^2 + y^2 <= a^2, where a > 0.

### Uppgift 8
Evaluate doubleintegral over Q of (x + y) dA, where Q is the quarter-disk x >= 0, y >= 0, x^2 + y^2 <= a^2, where a > 0.

### Uppgift 9
Evaluate doubleintegral over Q of e^(x^2+y^2) dA, where Q is the quarter-disk x >= 0, y >= 0, x^2 + y^2 <= a^2, where a > 0.

### Uppgift 10
Evaluate doubleintegral over Q of (2xy)/(x^2 + y^2) dA, where Q is the quarter-disk x >= 0, y >= 0, x^2 + y^2 <= a^2, where a > 0.

### Uppgift 11
Evaluate doubleintegral over S of (x + y) dA, where S is the region in the first quadrant lying inside the disk x^2 + y^2 <= a^2 and under the line y = sqrt(3)*x.

### Uppgift 12
Find doubleintegral over S of x dA, where S is the disk segment x^2 + y^2 <= 2, x >= 1.

### Uppgift 13
Evaluate doubleintegral over T of (x^2 + y^2) dA, where T is the triangle with vertices (0, 0), (1, 0), and (1, 1).

### Uppgift 14
Evaluate doubleintegral over {x^2+y^2 <= 1} of ln(x^2 + y^2) dA.

### Uppgift 15
Find the average distance from the origin to points in the disk x^2 + y^2 <= a^2.

### Uppgift 16
Find the average value of e^(-(x^2+y^2)) over the annular region 0 < a^2 <= x^2 + y^2 <= b^2.

### Uppgift 17
For what values of k, and to what value, does the integral
    doubleintegral over {x^2+y^2 >= 1} of (dA)/((x^2 + y^2)^k)
converge?

### Uppgift 18
For what values of k, and to what value, does the integral
    doubleintegral over R^2 of (dA)/((1 + x^2 + y^2)^k)
converge?

### Uppgift 19
Evaluate doubleintegral over D of xy dA, where D is the plane region satisfying x >= 0, 0 <= y <= x, and x^2 + y^2 <= a^2.

### Uppgift 20
Evaluate doubleintegral over C of y dA, where C is the upper half of the cardioid disk r <= 1 + cos(theta).

### Uppgift 21
Find the volume lying between the paraboloids z = x^2 + y^2 and 3z = 4 - x^2 - y^2.

### Uppgift 22
Find the volume lying inside both the sphere x^2 + y^2 + z^2 = a^2 and the cylinder x^2 + y^2 = ax.

### Uppgift 23
Find the volume lying inside both the sphere x^2 + y^2 + z^2 = 2a^2 and the cylinder x^2 + y^2 = a^2.

### Uppgift 24
Find the volume of the region lying above the xy-plane, inside the cylinder x^2 + y^2 = 4, and below the plane z = x + y + 4.

### Uppgift 25
[!] Find the volume of the region lying inside all three of the circular cylinders x^2 + y^2 = a^2, x^2 + z^2 = a^2, and y^2 + z^2 = a^2. Hint: Make a good sketch of the first octant part of the region, and use symmetry whenever possible.

### Uppgift 26
Find the volume of the region lying inside the circular cylinder x^2 + y^2 = 2y and inside the parabolic cylinder z^2 = y.

### Uppgift 27
[!] Many points are chosen at random in the disk x^2 + y^2 <= 1. Find the approximate average value of the distance from these points to the nearest side of the smallest square that contains the disk.

### Uppgift 28
Find the average value of x over the segment of the disk x^2 + y^2 <= 4 lying to the right of x = 1. What is the centroid of the segment?

### Uppgift 29
Find the volume enclosed by the ellipsoid (x^2/a^2) + (y^2/b^2) + (z^2/c^2) = 1.

### Uppgift 30
Find the volume of the region in the first octant below the paraboloid
    z = 1 - x^2/a^2 - y^2/b^2.
Hint: Use the change of variables x = au, y = bv.

### Uppgift 31
[!] Evaluate doubleintegral over {|x|+|y| <= a} of e^(x+y) dA.

### Uppgift 32
Find doubleintegral over P of (x^2 + y^2) dA, where P is the parallelogram bounded by the lines x + y = 1, x + y = 2, 3x + 4y = 5, and 3x + 4y = 6.

### Uppgift 33
Find the area of the region in the first quadrant bounded by the curves xy = 1, xy = 4, y = x, and y = 2x.

### Uppgift 34
Evaluate doubleintegral over R of (x^2 + y^2) dA, where R is the region in the first quadrant bounded by y = 0, y = x, xy = 1, and x^2 - y^2 = 1.

### Uppgift 35
[!] Let T be the triangle with vertices (0, 0), (1, 0), and (0, 1). Evaluate the integral
    doubleintegral over T of e^((y-x)/(y+x)) dA
(a) by transforming to polar coordinates, and
(b) by using the transformation u = y - x, v = y + x.

### Uppgift 36
Use the method of Example 7 to find the area of the region inside the ellipse 4x^2 + 9y^2 = 36 and above the line 2x + 3y = 6.

### Uppgift 37
[CAS] (The error function) The error function, Erf(x), is defined for x >= 0 by
    Erf(x) = (2/sqrt(pi)) * integral(0 to x) e^(-t^2) dt.
Show that
    Erf(x) = (4/sqrt(pi)) * integral(0 to pi/4) [1 - e^(-x^2/cos^2(theta))] dtheta.
Hence, deduce that Erf(x) <= 1 - e^(-x^2).

### Uppgift 38
[CAS] (The gamma and beta functions) The gamma function Gamma(x) and the beta function B(x, y) are defined by
    Gamma(x) = integral(0 to oo) t^(x-1) * e^(-t) dt,    (x > 0),
    B(x, y) = integral(0 to oo) t^(x-1) * (1 - t)^(y-1) dt,    (x > 0, y > 0).

The gamma function satisfies
    Gamma(x + 1) = x * Gamma(x)    and    Gamma(n + 1) = n!,    (n = 0, 1, 2, ...).

Deduce the following further properties of these functions:
(a) Gamma(x) = 2 * integral(0 to oo) s^(2x-1) * e^(-s^2) ds,    (x > 0),
(b) Gamma(1/2) = sqrt(pi),    Gamma(3/2) = (1/2) * sqrt(pi),
(c) If x > 0 and y > 0, then
    B(x, y) = 2 * integral(0 to pi/2) cos^(2x-1)(theta) * sin^(2y-1)(theta) dtheta,
(d) B(x, y) = Gamma(x) * Gamma(y) / Gamma(x + y).

