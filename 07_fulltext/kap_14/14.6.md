# Section 14.6: Parametric Problems
> Studieanteckningar baserade på Adams & Essex, Calculus 9th ed, sid 791-798
> Kursens numrering (Adams 10:e uppl.) = 14.6. Motsvarar 13.6 i 9:e uppl., som texten är hämtad ur.

[sid 791]

In this section we will briefly examine three unrelated situations in which we want to differentiate a function with respect to a parameter rather than one of the basic variables of the function. Such situations arise frequently in mathematics and its applications.

## Differentiating Integrals with Parameters

The Fundamental Theorem of Calculus shows how to differentiate a definite integral with respect to the upper limit of integration:

    d/dx integral(a to x) f(t) dt = f(x).

We are going to look at a different problem about differentiating integrals. If the integrand of a definite integral also depends on variables other than the variable of integration, then the integral will be a function of those other variables. How are we to find the derivative of such a function? For instance, consider the function F(x) defined by

    F(x) = integral(a to b) f(x,t) dt.

We would like to be able to calculate F'(x) by taking the derivative inside the integral:

    F'(x) = d/dx integral(a to b) f(x,t) dt = integral(a to b) (∂/∂x) f(x,t) dt.

Observe that we use d/dx outside the integral and ∂/∂x inside; this is because the integral is a function of x only, but the integrand f is a function of both x and t. If the integrand depends on more than one parameter, then partial derivatives would be needed inside and outside the integral:

    ∂/∂x integral(a to b) f(x,y,t) dt = integral(a to b) (∂/∂x) f(x,y,t) dt.

The operation of taking a derivative with respect to a parameter inside the integral, or differentiating through the integral, as it is usually called, seems plausible. We differentiate sums term by term, and integrals are the limits of sums. However, both the differentiation and integration operations involve the taking of limits (limits of Newton quotients for derivatives, limits of Riemann sums for integrals). Differentiating through the integral requires changing the order in which the two limits are taken and, therefore, requires justification.

We have already seen another example of change of order of limits. When we assert that two mixed partial derivatives with respect to the same variables are equal,

    ∂^2 f / ∂x∂y = ∂^2 f / ∂y∂x,

we are, in fact, saying that limits corresponding to differentiation with respect to x and y can be taken in either order with the same result. This is not true in general; we proved it under the assumption that both of the mixed partials were continuous. (See Theorem 1 and Exercise 16 of Section 12.4.) In general, some assumptions are required to justify the interchange of limits. The following theorem gives one set of conditions that justify the interchange of limits involved in differentiating through the integral.

## THEOREM 6 -- Differentiating through an integral

Suppose that for every x satisfying c < x < d, the following conditions hold:
- (i) the integrals
    
    integral(a to b) f(x,t) dt   and   integral(a to b) f_1(x,t) dt
    
    both exist (either as proper or convergent improper integrals).
- (ii) f_11(x,t) exists and satisfies
    
    |f_11(x,t)| <= g(t),    a < t < b,
    
    where
    
    integral(a to b) g(t) dt = K < ∞.

Then for each x satisfying c < x < d, we have

    d/dx integral(a to b) f(x,t) dt = integral(a to b) (∂/∂x) f(x,t) dt.

### PROOF

Let
    F(x) = integral(a to b) f(x,t) dt.

If c < x < d, h ≠ 0, and |h| is sufficiently small that c < x + h < d, then, by Taylor's Formula,

    f(x + h, t) = f(x,t) + h f_1(x,t) + (h^2/2) f_11(x + ξh, t)

for some ξ between 0 and 1. Therefore,

    |F(x + h) - F(x) / h - integral(a to b) f_1(x,t) dt|
    = |integral(a to b) [f(x+h,t) - f(x,t)] / h dt - integral(a to b) f_1(x,t) dt|
    <= integral(a to b) |[f(x+h,t) - f(x,t)] / h - f_1(x,t)| dt
    = integral(a to b) |(h/2) f_11(x + ξh, t)| dt
    <= (h/2) integral(a to b) g(t) dt = (Kh)/2 -> 0 as h -> 0.

Therefore,

    F'(x) = lim(h->0) [F(x+h) - F(x)] / h = integral(a to b) f_1(x,t) dt,

which is the desired result.

Remark It can be shown that the conclusion of Theorem 6 also holds under the sole assumption that f_1(x,t) is continuous on the closed, bounded rectangle c <= x <= d, a <= t <= b. We cannot prove this here; the proof depends on a subtle property called uniform continuity possessed by continuous functions on closed bounded sets in R^n. (See Appendix IV for the case n = 1.) In any event, Theorem 6 is more useful for our purposes because it allows for improper integrals.

[sid 792]

## EXAMPLE 1

Evaluate integral(0 to ∞) t^n e^(-t) dt.

Solution Starting with the convergent improper integral

    integral(0 to ∞) e^(-s) ds = lim(R->∞) [-e^(-s)]_0^R = lim(R->∞) (1 - e^(-R)) = 1,

we introduce a parameter by substituting s = xt, ds = x dt (where x > 0) and get

    integral(0 to ∞) e^(-xt) dt = 1/x.

Now differentiate n times (each resulting integral converges):

    integral(0 to ∞) (-t) e^(-xt) dt = -1/x^2,
    integral(0 to ∞) (-t)^2 e^(-xt) dt = (-1)^2 * 2/x^3,
    ...
    integral(0 to ∞) (-t)^n e^(-xt) dt = (-1)^n * n! / x^(n+1).

Putting x = 1, we get

    integral(0 to ∞) t^n e^(-t) dt = n!.

Note that this result could be obtained by integration by parts (n times) or a reduction formula. This method is a little easier.

Remark The reader should check that the function f(x,t) = t^k e^(-xt) satisfies the conditions of Theorem 6 for x > 0 and k >= 0. We will normally not make a point of this.

## EXAMPLE 2

Evaluate F(x,y) = integral(0 to ∞) [e^(-xt) - e^(-yt)] / t dt for x > 0; y > 0.

Solution We have

    ∂F/∂x = -integral(0 to ∞) e^(-xt) dt = -1/x      and      ∂F/∂y = integral(0 to ∞) e^(-yt) dt = 1/y.

It follows that

    F(x,y) = -ln x + C_1(y)      and      F(x,y) = ln y + C_2(x).

Comparing these two formulas for F, we are forced to conclude that

    C_1(y) = ln y + C

for some constant C. Therefore,

    F(x,y) = ln y - ln x + C = ln(y/x) + C.

Since F(1,1) = 0, we must have C = 0 and F(x,y) = ln(y/x).

Remark We can combine Theorem 6 and the Fundamental Theorem of Calculus to differentiate an integral with respect to a parameter that appears in the limits of integration as well as in the integrand. If

    F(x, b, a) = integral(a to b) f(x,t) dt,

then, by the Chain Rule,

    d/dx [F(x, b(x), a(x))] = ∂F/∂x + (∂F/∂b)(db/dx) + (∂F/∂a)(da/dx).

Accordingly, we have

    d/dx integral(a(x) to b(x)) f(x,t) dt
    = integral(a(x) to b(x)) (∂/∂x) f(x,t) dt + f(x, b(x)) b'(x) - f(x, a(x)) a'(x).

We require that a(x) and b(x) be differentiable at x, and for the application of Theorem 6, that a <= a(x) <= b and a <= b(x) <= b for all x satisfying c < x < d.

[sid 793]

## EXAMPLE 3

Solve the integral equation f(x) = a - integral(b to x) (x - t) f(t) dt.

Solution Assume, for the moment, that the equation has a sufficiently well-behaved solution to allow for differentiation through the integral. Differentiating twice, we get

    f'(x) = -(x - x)f(x) - integral(b to x) f(t) dt = -integral(b to x) f(t) dt,
    f''(x) = -f(x).

The latter equation is the differential equation of simple harmonic motion. Observe that the given equation for f and that for f' imply the initial conditions

    f(b) = a      and      f'(b) = 0.

Accordingly, we write the general solution of f''(x) = -f(x) in the form

    f(x) = A cos(x - b) + B sin(x - b).

The initial conditions then imply A = a and B = 0, so the required solution is f(x) = a cos(x - b). Finally, we note that this function is indeed smooth enough to allow the differentiations through the integral and is, therefore, the solution of the given integral equation. (If you wish, verify it in the integral equation.)

## Envelopes

An equation f(x,y,c) = 0 that involves a parameter c as well as the variables x and y represents a family of curves in the xy-plane. Consider, for instance, the family

    f(x,y,c) = x/c + cy - 2 = 0.

This family consists of straight lines with intercepts (2c, 2/c) on the coordinate axes. Several of these lines are sketched in Figure 14.21. It appears that there is a curve to which all these lines are tangent. This curve is called the envelope of the family of lines.

In general, a curve C is called the envelope of the family of curves with equations f(x,y,c) = 0 if, for each value of c, the curve f(x,y,c) = 0 is tangent to C at some point depending on c.

For the family of lines in Figure 14.21 it appears that the envelope may be the rectangular hyperbola xy = 1. We will verify this after developing a method for determining the equation of the envelope of a family of curves. We assume that the function f(x,y,c) has continuous first partials and that the envelope is a smooth curve.

[FIGUR 14.21: A family of straight lines with different slopes and intercepts, showing an envelope curve. The lines are labeled with different c values.]

For each c, the curve f(x,y,c) = 0 is tangent to the envelope at a point (x,y) that depends on c. Let us express this dependence in the explicit form x = g(c); y = h(c); these equations are parametric equations of the envelope. Since (x,y) lies on the curve f(x,y,c) = 0, we have

    f(g(c), h(c), c) = 0.

Differentiating this equation with respect to c, we obtain

    f_1 g'(c) + f_2 h'(c) + f_3 = 0,                          (*)

where the partials of f are evaluated at (g(c), h(c), c).

The slope of the curve f(x,y,c) = 0 at (g(c), h(c), c) can be obtained by differentiating its equation implicitly with respect to x:

    f_1 + f_2 dy/dx = 0.

On the other hand, the slope of the envelope x = g(c); y = h(c) at that point is dy/dx = h'(c)/g'(c). Since the curve and the envelope are tangent at (g(c), h(c), c), these slopes must be equal. Therefore,

    f_1 + f_2 h'(c)/g'(c) = 0,    so    f_1 g'(c) + f_2 h'(c) = 0.

Combining this with equation (*) we get f_3(x,y,c) = 0 at all points of the envelope.

The equation of the envelope can be found by eliminating c between the two equations

    f(x,y,c) = 0      and      (∂/∂c) f(x,y,c) = 0.

[sid 794]

## EXAMPLE 4

Find the envelope of the family of straight lines f(x,y,c) = x/c + cy - 2 = 0.

Solution We eliminate c between the equations

    f(x,y,c) = x/c + cy - 2 = 0      and      f_c(x,y,c) = -x/c^2 + y = 0.

These equations can be easily solved and give x = c and y = 1/c. Hence, they imply that the envelope is xy = 1, as we conjectured earlier.

## EXAMPLE 5

Find the envelope of the family of circles (x - c)^2 + y^2 = c.

Solution Here, f(x,y,c) = (x - c)^2 + y^2 - c. The equation of the envelope is obtained by eliminating c from the pair of equations

    f(x,y,c) = (x - c)^2 + y^2 - c = 0,
    (∂/∂c) f(x,y,c) = -2(x - c) - 1 = 0.

From the second equation, x = c - 1/2, and then from the first, y^2 = c - 1/4. Hence, the envelope is the parabola

    x = y^2 - 1/4.

This envelope and some of the circles in the family are sketched in Figure 14.22.

[FIGUR 14.22: A family of circles (x - c)^2 + y^2 = c for different values of c, with their envelope shown as a parabola.]

A similar technique can be used to find the envelope of a family of surfaces. This will be a surface tangent to each member of the family.

[sid 795]

## EXAMPLE 6

(The Mach cone) Suppose that sound travels at speed c in still air and that a supersonic aircraft is travelling at speed v > c along the x-axis, so that its position at time t is (vt, 0, 0). Find the envelope at time t of the sound waves created by the aircraft at previous times. See Figure 14.23.

Solution The sound created by the aircraft at time τ < t spreads out as a spherical wave front at speed c. The centre of this wave front is (vτ, 0, 0), the position of the aircraft at time τ. At time t the radius of this wave front is c(t - τ), so its equation is

    f(x,y,z,τ) = (x - vτ)^2 + y^2 + z^2 - c^2(t - τ)^2 = 0.      (*)

At time t the envelope of all these wave fronts created at earlier times τ is obtained by eliminating the parameter τ from the above equation and the equation

    (∂/∂τ) f(x,y,z,τ) = -2v(x - vτ) + 2c^2(t - τ) = 0.

Solving this latter equation for τ, we get τ = (vx - c^2 t) / (v^2 - c^2). Thus,

    x - vτ = x - v(vx - c^2 t)/(v^2 - c^2) = c^2(vt - x)/(v^2 - c^2),
    t - τ = t - (vx - c^2 t)/(v^2 - c^2) = v(vt - x)/(v^2 - c^2).

[FIGUR 14.23: A cone-shaped diagram showing the Mach cone formed by sound waves from a supersonic aircraft traveling along the x-axis.]

We substitute these two expressions into equation (*) to eliminate τ:

    c^4 / (v^2 - c^2)^2 * (vt - x)^2 + y^2 + z^2 - c^2 v^2 / (v^2 - c^2)^2 * (vt - x)^2 = 0
    y^2 + z^2 = c^2 / (v^2 - c^2)^2 * (v^2 - c^2) * (vt - x)^2 = c^2 / (v^2 - c^2) * (vt - x)^2.

The envelope is the cone

    x = vt - sqrt(v^2 - c^2)/c * sqrt(y^2 + z^2),

which extends backward in the x direction from its vertex at (vt, 0, 0), the position of the aircraft at time t. This is called the Mach cone. The sound of the aircraft cannot be heard at any point until the cone reaches that point.

## Equations with Perturbations

In applied mathematics one frequently encounters intractable equations for which at least approximate solutions are desired. Sometimes such equations result from adding an extra term to what would otherwise be a simple and easily solved equation. This extra term is called a perturbation of the simpler equation. Often the perturbation has a coefficient smaller than the other terms in the equation; that is, it is a small perturbation. If this is the case, you can find approximate solutions to the perturbed equation by replacing the small coefficient by a parameter and calculating Maclaurin polynomials in that parameter. One example should serve to clarify the method.

[sid 796]

## EXAMPLE 7

Find an approximate solution of the equation y + (1/50) ln(1 + y) = x^2.

Solution Without the logarithm term, the equation would clearly have the solution y = x^2. Let us replace the coefficient 1/50 with the parameter ε and look for a solution y = y(x, ε) to the equation

    y + ε ln(1 + y) = x^2                                                          (*)

in the form

    y = y(x, ε) = y(x, 0) + ε y_ε(x, 0) + (ε^2/2!) y_εε(x, 0) + O(ε^3),

where the subscripts ε denote derivatives with respect to ε. We shall calculate the terms up to second order in ε. Evidently y(x, 0) = x^2. Differentiating equation (*) twice with respect to ε and evaluating the results at ε = 0, we obtain

    ∂y/∂ε + ln(1 + y) + y/(1 + y) * ∂y/∂ε = 0,
    ∂^2y/∂ε^2 + 2/(1 + y) * ∂y/∂ε + ∂/(∂ε)[1/(1 + y)] * ∂y/∂ε = 0,

    y_ε(x, 0) = -ln(1 + x^2),
    y_εε(x, 0) = 2/(1 + x^2) * ln(1 + x^2).

Hence,

    y(x, ε) = x^2 - ε ln(1 + x^2) + (ε^2/2) * [2 ln(1 + x^2) / (1 + x^2)] + O(ε^3),

and the given equation has the approximate solution

    y ≈ x^2 - ln(1 + x^2)/50 + ln(1 + x^2)/(2500(1 + x^2)).

Similar perturbation techniques can be used for systems of equations and for differential equations.

[sid 797]

[sid 798]

## Exercises 14.6

### Uppgift 1
Let F(x) = integral(0 to 1) t^x dt = 1/(x + 1) for x > -1. By repeated differentiation of F, evaluate the integral integral(0 to 1) t^x (ln t)^n dt.

### Uppgift 2
By replacing t with xt in the well-known integral integral(-∞ to ∞) e^(-t^2) dt = sqrt(π), and differentiating with respect to x, evaluate integral(-∞ to ∞) t^2 e^(-t^2) dt and integral(-∞ to ∞) t^4 e^(-t^2) dt.

### Uppgift 3
Evaluate integral(-∞ to ∞) [e^(-xt^2) - e^(-yt^2)] / t^2 dt for x > 0; y > 0.

### Uppgift 4
Evaluate integral(0 to 1) [t^x - t^y] / ln t dt for x > -1; y > -1.

### Uppgift 5
Given that integral(0 to ∞) e^(-xt) sin t dt = 1 / (1 + x^2) for x > 0 (which can be shown by integration by parts), evaluate integral(0 to ∞) t e^(-xt) sin t dt and integral(0 to ∞) t^2 e^(-xt) sin t dt.

### Uppgift 6
[!] Referring to Exercise 5, for x > 0 evaluate F(x) = integral(0 to ∞) e^(-xt) (sin t / t) dt. Show that limx→∞ F(x) = 0 and hence evaluate the integral integral(0 to ∞) (sin t / t) dt = lim(x→0) F(x).

### Uppgift 7
Evaluate integral(0 to ∞) dt / (x^2 + t^2) and use the result to help you evaluate integral(0 to ∞) dt / (x^2 + t^2)^2 and integral(0 to ∞) dt / (x^2 + t^2)^3.

### Uppgift 8
[!] Evaluate integral(0 to x) dt / (x^2 + t^2) and use the result to help you evaluate integral(0 to x) dt / (x^2 + t^2)^2 and integral(0 to x) dt / (x^2 + t^2)^3.

### Uppgift 9
Find f^((n+1))(a) if f(x) = 1 + integral(a to x) (x - t)^n f(t) dt.

### Uppgift 10
Solve the integral equation f(x) = C + x + D + integral(0 to x) (x - t) f(t) dt.

### Uppgift 11
Solve the integral equation f(x) = x + integral(0 to x) (x - 2t) f(t) dt.

### Uppgift 12
Solve the integral equation f(x) = 1 + integral(0 to ∞) (x + t) f(t) dt.

### Uppgift 13
Find the envelope of the family of curves y = 2cx - c^2.

### Uppgift 14
Find the envelope of the family of curves y - (x - c) cos c = sin c.

### Uppgift 15
Find the envelope of the family of curves x cos c + y sin c = 1.

### Uppgift 16
Find the envelope of the family of curves x/cos c + y/sin c = 1.

### Uppgift 17
Find the envelope of the family of curves y = c + (x - c)^2.

### Uppgift 18
Find the envelope of the family of circles (x - c)^2 + (y - c)^2 = 1.

### Uppgift 19
Does every one-parameter family of curves in the plane have an envelope? Try to find the envelope of y = x^2 + c.

### Uppgift 20
For what values of k does the family of circles x^2 + (y - c)^2 = kc^2 have an envelope?

### Uppgift 21
Try to find the envelope of the family y^3 = (x + c)^2. Are the curves of the family tangent to the envelope? What have you actually found in this case? Compare with Example 3 of Section 14.3.

### Uppgift 22
[A] Show that if a two-parameter family of surfaces f(x,y,z,α,β) = 0 has an envelope, then the equation of that envelope can be obtained by eliminating α and β from the three equations

    f(x,y,z,α,β) = 0,
    (∂/∂α) f(x,y,z,α,β) = 0,
    (∂/∂β) f(x,y,z,α,β) = 0.

### Uppgift 23
Find the envelope of the two-parameter family of planes x sin α cos β + y sin α sin β + z cos α = 1.

### Uppgift 24
Find the envelope of the two-parameter family of spheres (x - α)^2 + (y - β)^2 + z^2 = (α^2 + β^2) / 2.

### Uppgift 25
Find the terms up to second power in ε in the solution y of the equation y + ε sin y = x.

### Uppgift 26
Find the terms up to second power in ε in the solution y of the equation y^2 + ε e^(-y) = 1 + x^2.

### Uppgift 27
Find the terms up to second power in ε in the solution y of the equation 2y + ε x / (1 + y^2) = 1.

### Uppgift 28
Use perturbation methods to evaluate y with error less than 10^(-8) given that y + (y^5/100) = 1/2.

### Uppgift 29
[!] Use perturbation methods to find approximate values for x and y from the system x + 2y + (e^(1-x)/100) = 3, x - y + (e^(1-y)/100) = 0. Calculate all terms up to second order in ε = 1/100.
